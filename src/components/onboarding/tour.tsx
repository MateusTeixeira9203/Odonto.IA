'use client';

/**
 * @deprecated O tour agora é gerenciado pelo DexWidget.
 * Este componente é mantido apenas para não quebrar imports existentes.
 */
export function OnboardingTour(): null {
  return null;
}
