# Handoff — 2026-06-24 06:31

## O que trabalhamos
- Aplicamos a **Fase 1 do Playbook (retenção)** no Odonto.IA. Plano completo aprovado em `C:\Users\mateu\.claude\plans\snug-mixing-hopcroft.md` (6 workstreams: E persona, A onboarding, F DEX, B loop/recompensa, C régua de e-mails, D relatório de valor).
- **Concluímos E, A, F e B.** C e D não começaram.
- Decisões de produto travadas com o fundador: persona perguntada pelo "job" (não idade); ordem `cadastro → identidade+persona → aha → plano → procedimentos → sucesso`; manter a "cara" do DEX (não trocar por glyph abstrato) e dar **expressões**.
- Cadência combinada: **um workstream por vez**, eu reporto (o que mudou + visual) e o fundador aprova antes de seguir.

## O que concluímos
- **E — Camada de persona:** coluna `dentistas.foco_principal`, módulo `src/lib/persona.ts`, carregado no `getDentistaCached`. Pronto.
- **A — Onboarding reformulado:** nova máquina de passos, `iniciarOnboarding`/`definirPlano`, plano movido pra depois do aha, round-trip da demo (`?from=onboarding` → `?step=plano`), guard do layout corrigido via `clinicas.onboarding_completo`. Pronto.
- **F — DEX:** componente canônico `DexMark` (SVG, 4 expressões: neutro/pensando/feliz/atento), `DexFace`/`DexAvatar` viraram wrappers (zero quebra), apresentação integrada ao aha, intro duplicado do dashboard removido. Pronto.
- **B — Loop do Hook:** recompensa pós-ficha com DEX feliz + linha por persona (veterano = "≈X min que não digitou"; iniciante = "pronta pra apresentar"), DEX "pensando" na detecção ao vivo, card de primeiros passos reenquadrado como "investimento" + ordem por persona. Pronto.

**Status geral:** Parcial — 4 de 6 workstreams. Tudo validado só com `typecheck` + `lint` (NÃO testado em app/DB rodando).

## Erros encontrados e como pensei em resolver
| Erro / problema | Causa (ou hipótese) | Como resolvi | Resolvido? |
|---|---|---|---|
| Onboarding chutava o user pro dashboard ao voltar da demo | `onboarding/layout.tsx` redirecionava se `dentista` existisse; no fluxo novo o dentista é criado no meio (pra demo rodar), então "existe" ≠ "concluído" | Migração 080 `clinicas.onboarding_completo`; layout só redireciona se o flag for true; flag setado no passo de procedimentos | sim |
| Lint: "component created during render" no DexMark | Defini `Eye` como componente dentro do render | Troquei por render-helper `renderEye(cx, key)` (função invocada, não componente) | sim |
| `node_modules` ausente / `tsc` fora do PATH | Repo sem deps instaladas | `npm install` (1min) + `npm run typecheck` | sim |
| Lint `set-state-in-effect` em `useDexGuide.ts` e `primeiros-passos-card.tsx` | Padrão pré-existente do codebase (não introduzido nesta sessão) | Deixado como está — não é nosso e o codebase inteiro tem | não (pré-existente, tolerável) |

## Arquivos alterados
| Arquivo | Mudança |
|---|---|
| supabase/migrations/...079_dentistas_foco_principal.sql | **novo** — coluna persona |
| supabase/migrations/...080_clinicas_onboarding_completo.sql | **novo** — flag de conclusão |
| src/lib/persona.ts | **novo** — tipo + copy por persona |
| src/lib/get-dentista.ts | carrega `foco_principal` |
| src/app/onboarding/actions.ts | `iniciarOnboarding` + `definirPlano` + marca `onboarding_completo` |
| src/app/onboarding/_components/onboarding-client.tsx | nova máquina de passos + persona + aha com DexMark |
| src/app/onboarding/page.tsx | `initialStep`/`focoInicial`/`nomeInicial` |
| src/app/onboarding/layout.tsx | guard via `onboarding_completo` |
| src/app/consulta/demo/page.tsx | prop `retornoOnboarding` (`?from=onboarding`) |
| src/app/consulta/[agendamentoId]/page.tsx | passa `dentistaFoco` |
| src/app/consulta/[agendamentoId]/_components/consulta-client.tsx | recompensa por persona + expressões do DEX + retorno onboarding |
| src/components/dex/dex-mark.tsx | **novo** — identidade canônica com expressões |
| src/components/onboarding/dex-mascot.tsx | DexFace/DexMascot → wrappers do DexMark |
| src/components/ui/dex-avatar.tsx | DexAvatar → wrapper do DexMark |
| src/components/onboarding/dex-guide.tsx | removida a cena preta de welcome |
| src/hooks/useDexGuide.ts | novos users começam em `point_demo` |
| src/lib/onboarding-progress.ts | ordem dos passos por persona |
| src/components/dashboard/primeiros-passos-card.tsx | reframe "investimento" |
| src/app/dashboard/page.tsx | passa persona pro progresso |

## O que ficou pra próxima sessão
1. **[CRÍTICO]** Aplicar as migrações **079 e 080** no banco (`supabase db push` ou MCP). O código já faz `SELECT foco_principal` e `onboarding_completo` — **sem as colunas, as queries quebram em runtime.**
2. **[CRÍTICO]** Verificação E2E ainda não feita: rodar o app (`npm run dev`) e percorrer o onboarding nas DUAS personas, incl. round-trip da demo e o intro do DEX. Nada foi testado com app/DB de verdade — só typecheck/lint.
3. **[ALTO]** Workstream **C — régua de e-mails** (D1/D3/D7 hoje mortos + D14/D30): cron idempotente espelhando `run-reminders`, tabela `onboarding_email_log`, variantes por persona, `vercel.json`.
4. **[ALTO]** Workstream **D — relatório de valor** recorrente (Customer Success): service + template + cron + superfície in-app + `clinicas.ultimo_relatorio_valor_em`.
5. **[MÉDIO]** Passar A/F/B por `design-review` + `impeccable-design-polish` (regra 4 do setup), em dark e light.

## O que eu estava planejando / cogitando
- **Demo exige login** — decidimos `cadastro → aha` agora (demo pública pré-cadastro descartada). Se a conversão emperrar, reabrir essa decisão é a primeira alavanca.
- **Heurística de "minutos economizados"** (~180 car/min) é um chute defensável, mas grosseiro. Vale calibrar com dados reais depois, ou suavizar a alegação se soar exagerada.
- **Card de primeiros passos:** o plano pedia "suavizar o auto-dismiss" (re-open discreto). NÃO fiz — achei over-engineering. Decisão em aberto: se o fundador quiser, adicionar um ponto de reentrada discreto.
- **Expressões do DEX** só ligadas no Modo Consulta e no aha. Dá pra espalhar (loaders, widget, briefings) — mas só onde reduzir atrito, não decorativo.
- **Régua de e-mails (C):** confirmar se o deploy é Vercel (não há `vercel.json`) antes de assumir Vercel Cron; senão, cron externo batendo no endpoint com `CRON_SECRET`.

## Como retomar
```bash
cd "C:/Users/mateu/Desktop/Odonto.IA-main"
# 1. aplicar migrações pendentes (079, 080) no banco — PRÉ-REQUISITO
# 2. validar:
npm run typecheck
npm run dev   # percorrer onboarding nas 2 personas
# 3. seguir pro Workstream C (régua de e-mails) — ver plano em
#    C:/Users/mateu/.claude/plans/snug-mixing-hopcroft.md
```

## Dívidas técnicas registradas
- [ ] Migrações 079/080 não aplicadas ao banco — pré-requisito de runtime (ver item 1).
- [ ] Verificação E2E pendente — só typecheck/lint até agora.
- [ ] `set-state-in-effect` pré-existente em `useDexGuide.ts:29` e `primeiros-passos-card.tsx:33` — tolerável (padrão do codebase), não introduzido aqui.
- [ ] "Minutos economizados" é heurística — calibrar com dados reais quando houver.
