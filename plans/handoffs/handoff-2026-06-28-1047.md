# Handoff — 2026-06-28 10:47

> Continuação do handoff de 09:48. Aqui adicionamos **B + limpeza do F** e
> **commitamos tudo numa branch**. Estrutura Parte 1 / Parte 2 definida no plano.

## Plano de referência
- **Plano:** `C:\Users\mateu\.claude\plans\snug-mixing-hopcroft.md` — agora dividido em
  **PARTE 1** (Fase 1 A–F, terminar+testar+verificar) e **PARTE 2** (alterações novas,
  só começa após Parte 1 validada). Specs/decisões da discussão estão no fim do plano.

## O que concluímos nesta sessão (além do A)
- **B1 — recompensa pós-ficha por persona** (`consulta-client.tsx` + páginas real/demo):
  no "Ficha salva", veterano vê "≈ X min que você não digitou" (heurística chars/180);
  iniciante vê "Pronto pra apresentar". `dentistaFoco` plumbado até o ConsultaClient.
- **B2 — card de primeiros passos como "investimento"** + **ordem por persona**
  (`onboarding-progress.ts` recebe `foco` e reordena; veterano = consulta_real primeiro,
  iniciante = planejamento primeiro). Subtítulo "Cada passo deixa o sistema mais seu".
- **F (limpeza)** — removida a **cena preta de welcome** do DEX (`dex-guide.tsx`); novos
  usuários começam em `point_demo` (`useDexGuide`). A apresentação do DEX agora vive no
  aha; o overlay do dashboard era redundante.
- **Commit:** branch **`feat/fase1-onboarding-persona-loop`** (commit `2895f89`, 20 arquivos).
  Não está em `main` nem deployado — preserva o trabalho pra você testar antes de mergear.

**Status geral da Parte 1:** A ✅, B ✅, E ✅, F (funcional/limpeza) ✅ — **faltam C e D**.

## Decisões desta sessão (discussão de produto)
| Tema | Decisão |
|---|---|
| Estrutura | **Parte 1** (terminar Fase 1, você testa/verifica) → **Parte 2** (alterações novas). Gate explícito. |
| Billing | Trial **com cartão**, capturado **pós-aha** (no passo do plano). Provider **em aberto** (você verifica AbacatePay / talvez trocar). 7 ou 14 dias **a estudar**. |
| Modelo IA | Manter **Groq** (rápido = mágica). Alavanca do aha = **visual**, não modelo. |
| Escopo de hoje | A + B + limpeza F (o testável). C/D não dá pra testar local sem deploy. |

## Erros / notas
| Item | Nota |
|---|---|
| Lint: `set-state-in-effect` em `useDexGuide.ts:29` e `primeiros-passos-card.tsx:33` | **Pré-existente** (handoff anterior já registrou). Meu B/F não adiciona erro novo. typecheck limpo. |
| B/F não testados no browser | Você assumiu o teste manual ("eu só testo ao chegar"). typecheck-clean + committed. |
| Heurística "minutos" (chars/180) | Chute defensável — calibrar com dados reais depois. |

## Arquivos alterados (nesta leva, sobre o A)
| Arquivo | Mudança |
|---|---|
| src/hooks/useDexGuide.ts | novos users → `point_demo` (sem cena welcome) |
| src/components/onboarding/dex-guide.tsx | removida a cena preta de welcome |
| src/lib/onboarding-progress.ts | `getOnboardingProgresso(clinicaId, foco)` reordena por persona |
| src/components/dashboard/primeiros-passos-card.tsx | reframe "investimento" |
| src/app/dashboard/page.tsx | passa `foco_principal` ao progresso |
| src/app/consulta/[agendamentoId]/_components/consulta-client.tsx | recompensa por persona (`dentistaFoco`) |
| src/app/consulta/[agendamentoId]/page.tsx + demo/page.tsx | passam `dentistaFoco` |

## O que ficou pra próxima sessão (segunda 29/06 à noite)
1. **[VOCÊ, ao chegar]** Testar **A + B** no app (2 personas): onboarding completo, recompensa pós-ficha, card de primeiros passos por persona, sem overlay preto do DEX. Bug #2 (badge POPULAR) visual. Me passar os pontos a corrigir.
2. **[CRÍTICO]** Decidir: mergear a branch `feat/fase1-onboarding-persona-loop` em `main` (após teste).
3. **[ALTO] Terminar a Parte 1: C e D** — régua de e-mails + relatório de valor (cron/e-mail, verify-on-deploy). Precisa: decidir o gatilho do cron (Vercel? externo?).
4. **[MÉDIO]** Limpar 2 contas de teste na prod (`e2e-vet-0628@`, `e2e-ini-0628@example.com`) — SQL pronto, é só pedir.
5. **[depois]** PARTE 2: G (Modo Consulta), H (dicionário/precisão), I (secretária), J (convites), billing.

## O que eu estava cogitando
- **Recompensa na demo:** B1 hoje só aparece na consulta REAL (a demo tem o bloco próprio "Você viu o DEX em ação"). Dá pra levar a recompensa por persona pra demo também (reforça o aha) — não fiz pra manter escopo.
- **Ordem dos passos com foco=null:** tratei null como veterano (consulta_real primeiro), seguindo `getPersona`. Confirmar se faz sentido pra contas antigas.
- **C/D antes de H:** registrei no plano que a precisão (H, Parte 2) vem depois de C/D — risco de construir e-mail/relatório pra um produto que ainda erra a ficha. O seu teste da Parte 1 é o que decide se H sobe.

## Como retomar
```bash
cd "C:/Users/mateu/Desktop/Odonto.IA-main"
git checkout feat/fase1-onboarding-persona-loop
npm run dev   # testar A + B nas 2 personas
# se ok: merge em main; depois seguir Parte 1 → C e D
```

## Dívidas técnicas
- [ ] Branch não mergeada nem deployada — mergear cedo após teste.
- [ ] 2 contas de teste `@example.com` na prod — limpar.
- [ ] `set-state-in-effect` pré-existente — dívida do codebase, não desta sessão.
- [ ] Heurística "minutos economizados" — calibrar com dados reais.
