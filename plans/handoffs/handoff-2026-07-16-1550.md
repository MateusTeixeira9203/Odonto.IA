# Handoff — 2026-07-16 15:50

> Sessão longa, três fases: (1) **discussão** que matou a decisão travada do roadmap 3.1 e
> produziu um modelo novo de hierarquia; (2) **planejamento** — 2 specs escritas e aprovadas;
> (3) **execução parcial** — código da Spec 1 + Fatia A da Spec 3 escritos, **nada aplicado,
> nada commitado, nada verificado em runtime**.
>
> **Zero commits.** Tudo está no working tree. `git log` no topo continua `ca1b4a4` (15/07).

## Plano / spec de referência
- **Roadmap ativo:** `plans/roadmap/roadmap-3.1-2026-07-14.md` — **reescrito 2× nesta sessão**
  (453 → ~190 linhas). Agora é **mapa**: fila numerada + ponteiros pras specs.
- **Spec 1** — `plans/specs/2026-07-16-hierarquia-3.1-nucleo-clinico-spec.md` — **aprovada**,
  código escrito, **não aplicada**.
- **Spec 3** — `plans/specs/2026-07-16-protetico-marcar-retorno-spec.md` — **draft**, Fatia A
  já executada (fora da ordem — ver Desvios).
- **Odontograma v3** — `spec-modo-consulta-v3-odontograma.md` — **congelado por decisão do Mateus**.

## O que trabalhamos
1. **Discussão da hierarquia** — o Mateus, com dias de observação em clínica, descartou a Leitura B
   do roadmap e desenhou uma **terceira** leitura, mais simples que A e B.
2. **Auditoria pesada do banco de prod** — derrubou 3 afirmações do roadmap e revelou o dado que
   passou a guiar as decisões (ver §Erros e §Cogitando).
3. **Spec 1 escrita, aprovada e implementada** (migration 099 + harness reescrito + código de app).
4. **Spec 3 escrita** (protético + marcar retorno), com desenho fechado em conversa.
5. **Fatia A da Spec 3 executada** + 2 defeitos corrigidos ao final da sessão.
6. **Reorganização do `plans/`** — 5 specs arquivadas, CLAUDE.md editado (com aprovação).

## O que concluímos
**Status geral: PARCIAL — tudo escrito, nada verificado.**

✅ **Pronto (mas não verificado):**
- Migration **099** + harness **`matriz_acesso_clinico.sql`** (~65 asserções) escritos.
- Código da Spec 1: `dentista_id` explícito nas escritas clínicas, conflito de agenda nos 3 pontos,
  autoria visível na ficha, fix da escrita silenciosa.
- **Fatia A da Spec 3**: `nova-consulta-modal` → `marcar-retorno-modal`, botão morto removido,
  `retorno_sugerido` fora do schema da IA, toast com data + link.
- **Fail-open da RPC corrigido** (3 call sites) e **indicador de fora-da-janela** na agenda.
- `typecheck` + `lint` (24 erros pré-existentes, sem regressão) + `build` limpos.

❌ **NÃO feito:**
- **A migration 099 nunca rodou. O harness nunca rodou.** Nenhuma das ~65 asserções foi executada.
- Nada testado em navegador. Nada commitado. **Todos os gates de aceite das 2 specs em aberto.**

## Decisões tomadas
| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| **Registro clínico é da CLÍNICA; trabalho é do AUTOR; dinheiro e agenda privados** | Leitura A (tudo aberto) e Leitura B (silo + compartilhar) do roadmap | Mateus trouxe de dias em clínica. Mais simples que as duas: não precisa de botão "compartilhar" se a leitura já é aberta |
| **Spec 2 (co-autoria) MORTA** | Construir co-responsável + event-log de compartilhamento | Num event-log a co-autoria é **propriedade, não feature** — ninguém sobrescreve por construção. Não havia o que construir |
| **Uma ficha = um dentista** (timeline = histórico) | "Uma ficha com seções de vários dentistas" (pedido original) | `fichas.assinatura_url` é uma por ficha — a assinatura de X passaria a cobrir o trabalho de Y. Problema de prontuário, não técnico |
| **`is_clinic_staff()` na 099** | Deixar `belongs_to_active_clinic()` solto | O protético entra como membro na Spec 3 e herdaria o prontuário inteiro. Custo: 6 linhas numa migration não aplicada |
| **Protético: senha opcional, tabela própria** | Forçar troca (molde da secretária) / generalizar `must_change_password` | Risco proporcional ao acesso: ele vê uma lista de prazos cujo texto o dentista escreveu. `secretarias` fica intacta, zero refactor |
| **Protético "só sinaliza" atraso** | Informar nova data prevista | Decisão do Mateus. Matou `nova_data_prevista` do schema, do tipo e da action → virou invariante #7 |
| **A agenda do protético É a agenda do dentista** | Tabela de agenda separada | Ideia do Mateus, melhor que a minha: remarcação e cancelamento saem **de graça** |
| **Fail-open → falha fechado** | Deixar como estava | Trava sem override falhando aberto é pior que quebrar |
| **Odontograma v3 congelado** | Aprovar como fundação | Decisão do Mateus: *"quando chegar a hora de mexer, trabalho só com isso"* |

## Desvios do plano original
| Item | O que aconteceu | Impacto |
|---|---|---|
| Spec 1 §12: harness roda **antes** do código de app | Mateus optou por guardar sem aplicar (não há banco local) | **Registrado na §12.1 da spec.** Código escrito contra RLS nunca exercitada |
| Spec 3: "Fatia A entrega valor sozinha, executar antes da B" | Fatia A **executada**, mas **não sobe sozinha** — usa `criarAgendamento`, que chama a RPC que só existe na 099 | A independência valia no papel, não na árvore. **099 + Spec 1 + Fatia A sobem juntas** |
| Spec 1 aprovada com base na auditoria do **banco** | A leitura do **código** (na execução) revelou 3 coisas que mudaram o contrato | Spec atualizada (§2.1) **antes** de codar — sem re-trabalho |
| `atualizarAgendamento` não estava na spec | Adicionado o conflito de paciente nele | Spec atualizada primeiro. Reagendar seria a porta dos fundos da invariante |

## Erros encontrados e como pensei em resolver
| Erro / problema | Causa | Como resolvi | Resolvido? |
|---|---|---|---|
| 🐛 **Consulta marcada pelo perfil "some" da agenda** (reportado: *"erro silencioso, funciona normal, mas na agenda não tá lá"*) | A agenda filtra por mês (`.gte/.lte`) e abre no corrente sem `?mes=`. **Determinístico**, não aleatório | **Prova forense:** o único agendamento do banco criado num mês pra outro é o do Mateus — **28/04 → 14/05, `scheduled`, nunca tocado em 3 meses**. Descartei silo (12/12 batem), fuso (`buildClinicDatetime` ok), `status`, `revalidatePath`. Fix: toast com data + **indicador de fora-da-janela** | **Sim** — mas só verificado por query, não por tela |
| 🐛 **RPC de conflito falhava ABERTO** | Os 3 call sites desestruturavam só `data`, descartando `error`. RPC falha → `null` → `=== true` dá false → **agenda sem checagem** | Falha fechado: captura `error`, recusa. Pior no `criarEncaixe` — tem override, e o conflito de paciente é o que ele não pode furar | **Sim** — código, não testado |
| 🐛 **"Agendar retorno" não aparecia em 33 de 35 fichas** | Botão aninhado em `{evo.retornoSugerido && ...}`; só 2 fichas têm o campo. **Amarrado a um palpite da IA — mas quem decide o retorno é o dentista** | Removido. O `NovaConsultaModal` do perfil já fazia o trabalho e sempre aparece → virou "Marcar retorno" | **Sim** — código |
| ⚠️ **A migration 099 abriria o prontuário pro protético** | `belongs_to_active_clinic()` só checa **membership**, não papel. O silo antigo barrava papel novo **por acidente** (`is_own_clinical_record`) | `is_clinic_staff()` em 7 pontos. **Validei a asserção contra prod: ela acusou `tratamentos_select_clinic_members` como furo REAL** — era `belongs_to_active_clinic()` puro | **Sim** — na 099, não aplicada |
| ⚠️ **UPDATE barrado por RLS não retorna erro no Supabase** | Devolve sucesso com 0 linhas. Com update otimista, a tela afirma o que o banco negou | `.select()` + reverter em `FichasTab.updateProcStatus` e `saveSectionToDb`. Virou invariante #9 | **Sim** — código |
| **O roadmap virou depósito** (feedback do Mateus: *"não dá pra saber o que estamos fazendo"*) | Empilhei as decisões da sessão dentro dele; a Spec 3 duplicava ~180 linhas da spec própria | Reescrito 2×: 453 → ~190 linhas. **Roadmap = mapa, spec = conteúdo** — registrei a regra no CLAUDE.md | **Sim** |

## Arquivos alterados
> **Nada commitado.** `git log` no topo = `ca1b4a4` (15/07).

| Arquivo | Mudança |
|---|---|
| `supabase/migrations/20260716000000_099_*.sql` | **NOVO** — `is_clinic_staff()`, 4 colunas de autoria + backfill, 8 policies, `paciente_tem_conflito_agenda` |
| `supabase/tests/matriz_acesso_clinico.sql` | **NOVO** — substitui `silo_dois_dentistas.sql` (deletado). ~65 asserções |
| `plans/specs/2026-07-16-hierarquia-3.1-*.md` | **NOVO** — Spec 1, aprovada, 10 invariantes |
| `plans/specs/2026-07-16-protetico-marcar-retorno-spec.md` | **NOVO** — Spec 3, draft, 2 fatias |
| `plans/roadmap/roadmap-3.1-2026-07-14.md` | Reescrito 2× — virou mapa |
| `CLAUDE.md` | **Com aprovação:** convenção `plans/concluidos/` + "roadmap é mapa, spec é conteúdo" |
| `plans/concluidos/spec-fase1-{1..5}.md` | **Movidos** de `specs/` (com aprovação). Link do v3 corrigido |
| `src/app/dashboard/agendamentos/actions.ts` | Conflito de paciente nos 3 pontos + **fail-open corrigido** |
| `src/app/dashboard/agendamentos/page.tsx` + `_components/agendamentos-client.tsx` | **Indicador de fora-da-janela** |
| `.../modals/marcar-retorno-modal.tsx` | **NOVO** (ex-`nova-consulta-modal.tsx`, deletado) |
| `src/components/pacientes/FichasTab.tsx` | Autoria visível, `podeEditarFicha`, fix do update silencioso, botão morto removido |
| `src/hooks/usePlanejamentoPaciente.ts` | `dentistaId` + `.select()` no upsert |
| `src/app/api/dex/formatar-evolucao/route.ts` | `retorno_sugerido` fora do schema |
| + `consulta-client.tsx`, `consulta/actions.ts`, `consulta/page.tsx`, `paciente-detail-client.tsx`, `documentos-actions.ts`, `tratamento-actions.ts`, `ApresentarPaciente.tsx`, `DocumentosTab.tsx` | Propagação de `dentistaId` |

## O que ficou pra próxima sessão
1. **[CRÍTICO] Aplicar a migration 099** — SQL Editor do **DentAi** (`zenfemoxvwerplrjgfqz`).
   Agendado pelo Mateus pra **sábado 18/07 à tarde**. *(O roadmap chegou a dizer "sexta 17/07 à
   noite" — confirmar com ele qual vale.)*
2. **[CRÍTICO] Rodar `supabase/tests/matriz_acesso_clinico.sql`** — é o **gate**. Se reprovar, o
   código não sobe.
3. **[CRÍTICO] Commit + push** — 099 + Spec 1 + Fatia A **juntas**. ⚠️ **O código quebra em runtime
   sem a migration** (escreve `dentista_id` em 4 tabelas que não têm a coluna; o typecheck passa
   porque essas tabelas não são tipadas).
4. **[ALTO] Testar com 2 logins reais** — dentista A / dentista B / secretária. Inclui: aparência
   do indicador (dark mode, contraste) e o fail-open recusando de verdade.
5. **[MÉDIO] Escrever a Fatia B** (protético) — **só depois** da 099 aplicada. Todos os gates dela
   são comportamentais.
6. **[MÉDIO] Specs a escrever** (não dependem da 099): painel do Dex (#4), Job A (#5),
   transcrição tratada (#6).

## O que eu estava planejando / cogitando
- **O painel do Dex é o que eu puxaria primeiro** das 3 specs livres — ele é pré-requisito do
  keystone da Fatia B (o "pronto" que pinga o dentista). **Mas ele tem um problema antes de
  crescer: 0 de 48 notificações de dentista foram lidas em prod** (a secretária lê ~1/3 das dela).
  Isso não é "ele ignora", é sinal de que **não chega**. Adicionar uma 4ª fonte a um canal
  invisível dá 4 coisas invisíveis. **Descobrir a causa é o passo 1 da spec dela** — e eu não
  cheguei a investigar por quê.
- **Furo em `notificacoes` que eu não abri e não fechei:** a RLS filtra por `para_role` e **ignora
  `para_dentista_id`** — o dentista B vê a notificação endereçada ao A. Herdado (35 notificações já
  sofrem disso). Deixei pra spec do painel de propósito, mas **se a Fatia B subir antes, a
  atribuição de ficha vaza pra todos os dentistas**.
- **A varredura que eu não fiz:** `retorno_sugerido` tinha 2/35 fichas preenchidas e escondia um
  botão inteiro. **`fichas.conduta` tem exatamente os mesmos 2/35** — mesmo número, mesmo cheiro.
  Suspeito que haja mais feature morta atrás de campo que a IA raramente preenche. Vale um sweep
  ao mexer na ficha.
- **Sobre o parcelamento (shipado 14/07):** há **0 pagamentos com `data_vencimento` no banco** —
  a feature **nunca foi usada por ninguém** em 2 dias. Não levantei isso com o Mateus. Pode ser
  cedo, pode ser que ninguém achou. É a fonte "recebimentos" do painel do Dex, então a spec dele
  vai esbarrar nisso.
- **Tabelas mortas que não decidimos:** `planejamentos` (0), `planejamento_etapas` (0),
  `planejamento_procedimentos` (22 legadas, não é mais escrita), `tratamentos` (1 linha, 0 fichas
  vinculadas). A coluna nova em `planejamento_procedimentos` é **preventiva, não funcional** —
  mantive porque a policy velha joina em `pacientes.dentista_id`, que virou coluna zumbi.
- **A lição que a sessão inteira ensinou:** os 2 defeitos que corrigi no fim (fail-open, toast que
  mitiga sem consertar) **existiam porque a Spec 1 está escrita e nunca foi executada**. Cada dia
  que o código fica escrito e não verificado acumula defeito que só a execução revela. **Isso é o
  argumento mais forte pra a 099 não esperar além de sábado.**

## Como retomar
```bash
cd "C:/Users/mateu/Desktop/Odonto.IA-main"
git status --short        # ~25 arquivos, NADA commitado, nada staged
git log --oneline -1      # ca1b4a4 (15/07) — sessão inteira está no working tree
cat plans/roadmap/roadmap-3.1-2026-07-14.md   # seções "AGORA" e "A FILA" respondem tudo
```
**1º passo:** Mateus aplica `supabase/migrations/20260716000000_099_hierarquia_nucleo_clinico_compartilhado.sql`
no SQL Editor do DentAi. **2º:** rodar o harness. **Só se passar**, commitar.

## Dívidas técnicas registradas
- [ ] **Harness e migration 099 nunca rodaram** — todos os gates de 2 specs em aberto. Não há
      ambiente local (sem Docker/CLI/psql); o único banco é prod.
- [ ] **Aparência do indicador de fora-da-janela não verificada** — lógica validada por query
      (aberta em abril → "3, próximo em 14/05"); dark mode/contraste não. Precisa de dado futuro.
- [ ] **RLS de `notificacoes` ignora `para_dentista_id`** — dentista B vê notificação do A.
      Herdado. **Bloqueia a Fatia B se ela subir antes do painel do Dex.**
- [ ] **`atualizarAgendamento` não valida conflito de DENTISTA no servidor** — só o client checa.
      A Spec 1 fechou só o de *paciente*. POST direto ainda fura.
- [ ] **`pacientes.dentista_id` virou coluna zumbi** — `COMMENT` + invariante #5 mitigam.
- [ ] **Storage aberto por clínica** — assumido de propósito; o harness não cobre storage.
- [ ] 🔍 **Varrer features escondidas atrás de campo raramente preenchido pela IA** (`conduta`: 2/35).
- [ ] **Tabelas mortas** — decidir remover ou reviver quando o Job B for escrito.
- [ ] Herdadas: tipos `Pagamento`/`OrcamentoComItens` duplicados · Redis Upstash offline · senha
      vazada (HaveIBeenPwned) desligada · 24 eslint `set-state-in-effect` · 59 casts · `openai`
      órfão · bump next 16.x · contas de teste em prod.
