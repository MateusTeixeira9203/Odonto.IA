# Handoff — 21/07/2026, 03:50

## Plano / spec de referência
- **Roadmap ativo:** [`plans/roadmap/roadmap-A-plugins-especialidade-2026-07-20.md`](../roadmap/roadmap-A-plugins-especialidade-2026-07-20.md) — interrompe o 3.1
- **Spec:** [`plans/specs/spec-a0-fundacao-plugins-especialidade.md`](../specs/spec-a0-fundacao-plugins-especialidade.md) — **aprovada, parcialmente implementada**
- **Base canônica de design (NOVA, 21/07):** [`plans/specs/ficha-definitiva-2026-07-21-artefato.html`](../specs/ficha-definitiva-2026-07-21-artefato.html)
  — artefato publicado em `claude.ai/code/artifact/0b1caacb-cfe0-4b8b-8eeb-965ca91a9e8d`.
  **A implementação COPIA esta página.** Divergência = atualizar o artefato primeiro.
- Design anterior (odontograma/tokens): `DESIGN-ficha-a0.md` + `odontograma-v3-preview-dentistas-artefato.html` (segue válido pra paleta/tipografia)

## O que trabalhamos
1. **Fechamos o A0 code-complete** (fases 1–5): contrato de plugins, registry+detecção, rota de despacho 2-pass, orto end-to-end, ficha em 3 camadas.
2. **Migration 105 aplicada em prod** (`fichas.orto_manutencao jsonb`) — com OK explícito do Mateus.
3. **Ficha rápida passa a salvar `odontograma_eventos`** via a RPC atômica da consulta (decisão do Mateus) — a convergência começou.
4. **Redesign completo da ficha** contra o artefato definitivo — 2 rodadas de feedback do Mateus, a 2ª pedindo cópia literal ("não quero rastros do último").
5. **3 bugs corrigidos na última rodada:** SVG das faces cortando, status preso em "Planejado", Assinar no lugar errado.

## O que concluímos
**Status geral: Parcial** — A0 code-complete, mas **nada verificado ao vivo com dado real pelo Mateus**, e 2 itens do feedback bloqueados por migration.

✅ **Pronto e verificado (tsc+lint 100% limpos, mount ao vivo):**
- `src/lib/especialidades/{plugin,registry,orto}.ts` — contrato 5 peças, 8 plugins, detecção determinística (5/5 casos, unicidade 12 tipos/0 dup)
- `POST /api/dex/extrair-especialidade` — esqueleto 2-pass (401/403/400/`sem-extractor`); smoke test runtime 401/405
- Orto end-to-end: migration 105 aplicada+verificada, captura→save→load→render
- Ficha em 3 camadas no `FichasTab`, **redesenhada como organismo único** (campo mágico → tipo/data → odontograma faixa larga + chips de região → perfil do dente → registros → anotações/conduta)
- Ficha salva redesenhada: header 1 linha, colapsada compacta, expandida com odontograma-índice + perfil readOnly + registros
- **Merge de faces**: "Restauração M·45 / O·45 / L·45" → **"Restauração LMO · dente 45"** (criação e salva)
- Toggle de status planejado⇄realizado com action server-side (autoria + ficha assinada)
- Assinatura movida pro rodapé, listando os procedimentos que cobre
- Fix do SVG (sangria -15/115 nas zonas + `occlusalLabelPos`) — geometria provada por cálculo

⚠️ **Feito mas NUNCA visto funcionando com dado real:** todo o fluxo organize→salvar→§11 cards→perfil do dente. O Mateus testou e reportou problemas; corrigi 3, faltam 2 (bloqueados).

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| Ficha rápida **salva eventos** via `regravarEventosOdontograma` | Manter só modelo v2 | Um motor, duas molduras — camada 2 é impossível sem eventos |
| **Opção B**: odontograma-navegação substitui o seletor v2 | Opção A aditiva (feita antes) | A escolhi por segurança e o resultado ficou "frankenstein"; Mateus rejeitou |
| **Copiar o artefato 100%**, apagar o design antigo | Adaptar incrementalmente | Duas rodadas de adaptação geraram Frankenstein; ordem explícita do Mateus |
| Merge por **dente+tipo+status** com faces unidas | 1 card por evento (como o Dex emite) | 3 cards "Restauração X · 45" era poluição |
| Assinatura no **rodapé, por procedimento** | Botão "Assinar" no topo | Precisa mostrar O QUE cobre; planejado não assina |
| Sangria -15/115 nas zonas oclusais | Ajustar o contorno oval | O clipPath já resolve a forma; a borda reta é que vazava |

## Desvios do plano original

| Item do plano | O que aconteceu | Impacto |
|---|---|---|
| Spec A0: `PluginRender.camada` (singular) | Virou `camadas` (array) | Nenhum — dentística pinta 3 camadas; spec atualizada |
| Spec A0 §2.2: `PLUGINS` com push direto | Precisou de helper existencial `registrar()` | Nenhum — `TDetalhe` invariante em TS; spec atualizada |
| Spec A0: camada 1 = reusar `ToothDetailPanel` | Feito, mas **opção A → depois B** | Retrabalho; o seletor v2 saiu, `dentes_afetados` agora deriva dos eventos |
| Roadmap A: "orto de graça, dado já gravado" | **Falso** — nunca era persistido | Exigiu migration 105 |

## Erros encontrados e como pensei em resolver

| Erro / problema | Causa (ou hipótese) | Como resolvi / pretendo resolver | Resolvido? |
|---|---|---|---|
| Ficha não mudou nada ao abrir | Camadas guardadas por dado (I2) + fichas antigas sem eventos (sem backfill) | Expliquei; só ficha nova ganha o layout | sim (não era bug) |
| "Nada a ver com o artefato / frankenstein" | Fiz cirurgia incremental no layout legado em vez de transcrever | Reescrevi form (287→222 linhas) e card salvo (464→235) copiando o artefato | sim |
| SVG das faces cortando (pré-molar 44/45) | Contorno oval vai de x=16–84, mas as bandas M/D terminavam em x=0/100 → borda reta DENTRO do oval | Sangria -15/115 + `occlusalLabelPos` respeitando largura real | sim (por cálculo; **falta ver na tela**) |
| Status preso em "Planejado" | `RegistroCard` era read-only — não existia caminho de escrita | `onToggleStatus` + action `alternarStatusRegistro` (autoria + assinada + otimista/rollback) | sim (**não testado com clique real**) |
| "Não consigo mudar status naquela ficha" | **A ficha está ASSINADA** (03:42 BRT) — imutável por invariante #14 | Comportamento correto; testar em ficha nova | sim (não era bug) |
| Chips do perfil só selecionam, não abrem tabela | Evento não tem onde guardar dado estruturado | **Bloqueado: migration 106** (`detalhe jsonb`) | não |
| Sem botão de atribuir a outro dentista | Falta coluna `encaminhado_para` | **Bloqueado: migration 106** | não |
| Abas Permanentes/Decíduos duplicadas | Eu adicionei estáticas sobre as nativas do `Odontograma` | Removi as minhas | sim |
| Bloco "Procedimentos" sem sentido | Sobra do modelo v2 | Extinto; deriva dos eventos | sim |

## Arquivos alterados

| Arquivo | Mudança |
|---|---|
| `src/lib/especialidades/plugin.ts` | **NOVO** — contrato de 5 peças |
| `src/lib/especialidades/registry.ts` | **NOVO** — 8 plugins, detecção, `pluginPorId` |
| `src/lib/especialidades/orto.ts` | **NOVO** — 1º plugin (schema+detecta+Form+Card) |
| `src/components/fichas/registro-card.tsx` | **NOVO** — card §11 + observação + toggle de status |
| `src/components/fichas/orto-card.tsx` / `orto-form.tsx` | **NOVOS** — chips e form de orto |
| `src/app/api/dex/extrair-especialidade/route.ts` | **NOVO** — despacho 2-pass |
| `src/components/pacientes/FichasTab.tsx` | **Redesign completo** — organismo único + card salvo novo; código morto removido |
| `src/components/odontograma/tooth-geometry.ts` | Fix do corte + `occlusalLabelPos` |
| `src/components/odontograma/ToothDetailPanel.tsx` | Usa `occlusalLabelPos` |
| `src/app/consulta/[agendamentoId]/actions.ts` | **+`alternarStatusRegistro`** |
| `supabase/migrations/20260720000000_105_orto_manutencao_ficha.sql` | **NOVO — APLICADA em prod** |
| `plans/specs/ficha-definitiva-2026-07-21-artefato.html` | **NOVO — base canônica** |
| `plans/specs/spec-a0-fundacao-plugins-especialidade.md` | Desvios registrados |
| `plans/roadmap/roadmap-A-plugins-especialidade-2026-07-20.md` | Progresso das fases |

## O que ficou pra próxima sessão

1. **[CRÍTICO] Migration 106** — `odontograma_eventos.detalhe jsonb` + `encaminhado_para uuid`. Aditiva/nullable como a 105. **Gate: OK do Mateus.** Destrava a tabela do canal, os chips que abrem interface, e o encaminhamento.
2. **[CRÍTICO] Teste ao vivo do Mateus** em **ficha NOVA** (a de teste está assinada): organize → revise no odontograma → salve → §11 cards → clique no dente → toggle de status.
3. **[ALTO] Ver o SVG na tela** — fix validado por cálculo, não por pixel. Abrir o perfil do 44/45.
4. **[ALTO] Chips abrem interface** (pós-106) — endo→tabela de canais, implante→marca/medidas, conforme artefato §04.
5. **[MÉDIO] Perfil da REGIÃO** — chips de arcada/quadrante hoje só selecionam; deviam abrir painel análogo ao do dente (artefato §04).
6. **[MÉDIO] Decisões de spec pendentes:** assinatura por procedimento e `encaminhado_para` — o design já assume, a spec ainda não formalizou.
7. **[BAIXO] `grupo_id` no prompt** (Fase 2a) — precisa build de prod + Playwright pro eval.

## O que eu estava planejando / cogitando

- **A migration 106 deveria trazer as duas colunas juntas** (`detalhe` + `encaminhado_para`) — uma parada só em prod em vez de duas.
- **Formato do `detalhe`:** JSONB validado por Zod por plugin. Endo = `{canais:[{nome,referencia,comprimentoRaiz,ct,limaFinal}],obturacao,cimento}`. Preocupa-me a busca/filtro futura ("todos com lima #40") — resolvível com índice GIN depois, não agora.
- **Perfil da região:** cogitei reusar o `ToothDetailPanel` com âncora de região, mas ele é muito dente-específico (SVG+faces). Provavelmente componente próprio, mais simples (lista de procedimentos + chips + obs).
- **Fichas antigas nunca ganham o layout novo** (sem backfill, por decisão da spec). O Mateus pediu "atualize também as antigas" — o que fiz foi dar a elas o **mesmo visual** (header/expandido novos) com os dados v2 derivados. Se ele quiser eventos de verdade nelas, é backfill — decisão nova, e eu seria contra (inventar evento a partir de texto histórico).
- **O push segue adiado** pro fim do Roadmap A. O lote está grande (A0 inteiro + Job A + Fatia A). Continuo achando que um push intermediário reduziria risco, mas a decisão dele foi lote único — respeitado.
- **Dogfood do Job A (voz/anexo) nunca aconteceu** e já foi superado pelos eventos: o campo mágico está sendo usado de fato. Vale marcar como dívida em vez de gate.

## Como retomar
```bash
cd C:/Users/mateu/Desktop/Odonto.IA-main
git status --short                    # nada commitado ainda — trabalho todo no working tree
npx tsc --noEmit && npx eslint src/   # devem estar limpos
# abrir o artefato canônico ao lado do código:
#   plans/specs/ficha-definitiva-2026-07-21-artefato.html
# subir o dev e criar ficha NOVA (a de teste está assinada):
#   /dashboard/pacientes → paciente → Nova Evolução
```

## Dívidas técnicas registradas
- [ ] **Migration 106 não aplicada** — bloqueia tabela de especialidade e encaminhamento
- [ ] **Nada commitado** — sessão inteira no working tree; risco de perda
- [ ] `AssinaturaRecepcaoModal.tsx:34` — `set-state-in-effect` **pré-existente** (não meu), 1 erro de lint que sobra
- [ ] Fichas legadas usam derivação v2 no visual novo — sem eventos, sem perfil de dente rico
- [ ] `dentes_afetados`/`procedimentos` ainda vêm do Dex; a derivação a partir dos eventos no caminho manual não foi exercitada (orçamento/PDF dependem deles)
- [ ] Detecção de especialidades verificada por script-espelho, não pelo módulo real (sem runner TS no projeto) — eval permanente cabe na Fase 5
