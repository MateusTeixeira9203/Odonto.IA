# Handoff — 2026-06-30 15:21

## Plano / spec de referência
- **Plano ativo:** `plans/roadmap/plano-fase1-retencao.md` — ver §⭐ "Estado atual + Sprint hoje→amanhã".
- **Spec ativa:** `plans/specs/spec-KL-demo-apresentar.md` — **rascunho aguardando aprovação** (não codar antes do "ok" + design-brief).

## O que trabalhamos
1. **Testei o app inteiro via preview MCP** (2 personas + secretária + agendamento) — validação manual da Fase 1.
2. **Corrigi 2 bugs** achados no teste: balão flutuante do DEX no Modo Consulta + guard `must_change_password` da secretária.
3. **Discussão de produto (sem código):** redesenho da demo de onboarding (K) + elevar o "Apresentar" (L) — decisões travadas e escritas em spec.
4. **Reorganizei `plans/`** em subpastas (`handoffs/`, `specs/`, `roadmap/`) e atualizei todas as skills/agentes/ponteiros.
5. **Movi o plano mestre** de `.claude/plans/` pra dentro do projeto + tirei `plans/` do `.gitignore`.

## O que concluímos
- **Testes manuais (passaram):** onboarding veterano completo; secretária (criar → primeiro-acesso → dashboard); agendamento (criar paciente → agendar → status scheduled→confirmed→checked_in).
- **Bug 1 — balão DEX:** removido o coach flutuante (`consulta-client.tsx`). Duplicava o `DexAvatar` do header e cobria "Organizar com DEX". `tsc` limpo, reverificado no browser.
- **Bug 2 — guard secretária:** adicionado em `dashboard/layout.tsx` (`must_change_password` → `/primeiro-acesso`). Verificado: secretária agora troca senha antes do dashboard.
- **Spec K+L escrita e aprovada como rascunho.**
- **`plans/` reorganizado e religado** — glob do `session-start` testado e funcionando.

**Status geral:** Completo (a sessão fechou tudo que abriu; código commitado).

## Decisões tomadas
| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| Demo: simulação ensina, **sem slides** | 1-2 slides explicando o sistema | Mostrar > contar; slides têm baixo engajamento, atrasam o aha |
| Demo estende: ficha → assinatura mock → perfil demo → Apresentar (aha 2) | Parar em "Você viu o DEX em ação" | Mostra o end-state (ficha no prontuário) + 2º momento de valor |
| Elevar Apresentar em 2 lugares (tela "Ficha salva" + header do perfil) | Deixar só no contextual (3 cliques fundo) | É o 2º maior valor do produto, hoje subexposto |
| Apresentar do header com **seletor de fichas** (lógica de orçamentos) | Ir direto ao painel | Fora do contexto de uma ficha, precisa escolher qual apresentar |
| Conteúdo do Apresentar na demo: **mockado, dentista conduzindo** | IA ao vivo | Controlado, sempre bonito, sem falhar na frente do usuário novo |
| `plans/` em subpastas + dentro do projeto + fora do gitignore | Manter em `.claude/plans/`, tudo numa pasta | Organização + clicável + co-localizado com specs/handoffs |

## Desvios do plano original
Nenhum desvio de contrato. O sprint K+L é **adição** ao plano (Parte 1 já estava feita); C/D e Parte 2 ficaram explicitamente fora da janela.

## Erros encontrados e como pensei em resolver
| Erro / problema | Causa | Como resolvi | Resolvido? |
|---|---|---|---|
| Balão DEX cobre botão + 2 rostos na tela | Coach flutuante `fixed bottom-7 right-7` duplicava o `DexAvatar` do header | Removi o coach; header já é a identidade única do DEX | sim |
| Secretária entra no dashboard sem trocar senha | Faltava o guard `must_change_password` no `dashboard/layout.tsx` (só existia a página `/primeiro-acesso`) | Adicionei o check no layout → redirect | sim |
| Link do plano não abria | Plano morava em `.claude/plans/` (fora do projeto) | Movi pra `plans/roadmap/` + atualizei ponteiros | sim |

## Bugs abertos (não-bloqueantes, pro sprint)
- **Badge POPULAR** (tela de planos): só texto âmbar, sem estilo de pill.
- **Saudação "Dr. Dr."**: `nome.split(' ')[0]` duplica o prefixo quando o nome cadastrado começa com "Dr.".

## Arquivos alterados
| Arquivo | Mudança |
|---|---|
| src/app/consulta/[agendamentoId]/_components/consulta-client.tsx | removido balão flutuante do DEX + código morto (coachText, valuePhrase, guidePhase, DexFace) |
| src/app/dashboard/layout.tsx | guard `must_change_password` da secretária |
| .gitignore | removido `plans/` |
| CLAUDE.md | doc da estrutura `plans/` (+ bloco setup pré-existente) |
| plans/ (reorg) | subpastas handoffs/ specs/ roadmap/ + plano mestre movido pra cá |
| .claude/skills/{session-start,handoff,spec-driven}, .claude/agents/planner | caminhos atualizados pras subpastas (local, .claude/ é gitignored) |

## O que ficou pra próxima sessão
1. **[CRÍTICO]** Aprovar (ou ajustar) a spec `plans/specs/spec-KL-demo-apresentar.md` — não codar antes do "ok".
2. **[ALTO]** Decidir item 3.3 da spec: ficha do perfil demo **enlatada** vs **a que o usuário organizou** (sessionStorage — mais mágica).
3. **[ALTO]** Design-brief de K+L (UI nova — regra 4) → implementar **L primeiro**, depois **K** + os 2 bugs abertos.
4. **[MÉDIO]** Refazer todos os testes de hoje + os fluxos novos (ver checklist no plano).
5. **[BAIXO]** Limpar contas de teste na prod: `test-vet-0630@`, `test-ini-0630@`, `test-sec-0630@example.com`.
6. **[depois]** Persona iniciante completa + recompensa pós-ficha com áudio na consulta REAL (pendente do fundador).

## O que eu estava planejando / cogitando
- **Ficha do perfil demo via sessionStorage:** mostrar *o relato que o próprio usuário acabou de organizar* virando prontuário é mais mágico que enlatado — reforça "isso é real". Custa um pouco mais. É a decisão de abertura do item 2 acima.
- **CTA "Montar e apresentar o plano"** na tela "Ficha salva" pode disparar `generateFullPlanWithAI` a partir da ficha recém-salva — fluxo documentar→apresentar num clique. Vale testar se a IA rascunha bem direto da ficha.
- **Soft agents** (thinking-partner, business-strategist, obsidian) ainda dizem "ler handoffs em plans/" genérico — não atualizei (são tolerantes, leem recursivo). Se algum não achar handoff, é aqui.

## Como retomar
```bash
cd "C:/Users/mateu/Desktop/Odonto.IA-main"
git checkout feat/fase1-onboarding-persona-loop
# 1. revisar/aprovar plans/specs/spec-KL-demo-apresentar.md
# 2. design-brief de K+L → implementar L → K
npm run dev
```

## Dívidas técnicas registradas
- [ ] Branch `feat/fase1-onboarding-persona-loop` ainda não mergeada na `main`.
- [ ] `plans/` agora rastreável mas as edições em `.claude/` (skills/agentes) seguem gitignored → local-only.
- [ ] Contas de teste `@example.com` na prod — limpar.
- [ ] `set-state-in-effect` pré-existente (`useDexGuide.ts`, `primeiros-passos-card.tsx`) — dívida antiga do codebase.
