# Handoff — 21/07/2026, 14:20

## Plano / spec de referência
- **Mapa ativo:** [`plans/roadmap/roadmap-mestre-2026-07-21.md`](../roadmap/roadmap-mestre-2026-07-21.md) — **NOVO nesta sessão**, substitui `roadmap-3-fases`, `roadmap-3.1` e `roadmap-A` (os três em `concluidos/` com aviso no topo)
- **Prioridade fixada pelo Mateus:** ① tudo do dentista (ficha, odontograma, cockpit) → ② financeiro → ③ o resto
- **Spec nova aguardando aprovação:** [`spec-eventos-ficha-organizacao.md`](../specs/spec-eventos-ficha-organizacao.md)
- **Spec aplicada:** [`spec-106-detalhe-especialidade.md`](../specs/spec-106-detalhe-especialidade.md) — migration em prod

## O que trabalhamos
1. **Push do Roadmap A inteiro** (8 commits represados desde 20/07) + **migration 106 aplicada em prod**.
2. **Plugins de endo e implante** ponta a ponta, e depois a correção da tabela contra o artefato.
3. **Agenda: 6 correções**, três delas bugs silenciosos — **no ar** (`c4ff7bb`).
4. **Reorganização do `plans/`**: 3 roadmaps → 1 mapa; 24 specs → 16 (9 arquivadas).
5. **1º teste com anamnese real** — revelou perda massiva de dado clínico.

## O que concluímos

**Status geral: Parcial** — agenda fechada e no ar; ficha avançou mas com dívida grande registrada.

✅ **No ar e verificado:**
- Migration 106 (`detalhe jsonb`, `encaminhado_para`, +5 tipos, `nivel='boca'`) — verificada objeto a objeto
- Roadmap A completo em prod (`65cf9cc`)
- **Agenda (`c4ff7bb`)** — 6 correções, cada uma verificada por medição, não por leitura

🟡 **Commitado local, NÃO pushado (`8168b01`):**
- Tabela de endo fiel ao artefato + sinal derivado no painel do dente

⚠️ **Nunca verificado ao vivo:**
- Compartilhamento de ficha com **2 contas logadas** (RLS no ar desde 18/07, testada só por script)
- Tabelas de endo/implante com dado real salvando e recarregando

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| **Um roadmap mestre único** | Manter 3.1 + A separados | Os dois afirmavam "nada commitado / push adiado" depois do push — mapa que mente é pior que mapa nenhum |
| Prioridade: **dentista → financeiro → resto** | Financeiro primeiro (prod-down há 2 meses) | Decisão do Mateus; perda de dado clínico é irrecuperável, financeiro é recalculável |
| **Só a trava do DENTISTA ganha override** | Liberar as duas | Paciente em dois consultórios ao mesmo tempo, com a agenda do colega invisível — ninguém descobre até ele chegar |
| Janela de conflito **de véspera a dia seguinte** | Corrigir o limite do dia com offset | Não tentar acertar fuso: assim nenhum raciocínio de fronteira pode errar de novo |
| **Extrair** o algoritmo de faixas do Dia | Escrever a segunda cópia na Semana | Era a duplicação que a spec da ficha critica, na mesma sessão |
| Perio **não** entra na 106 | JSONB no evento | Roadmap A §A2 já decidira tabelas satélite (série temporal entre exames) |
| Assinatura por procedimento → **migration 107** | Junto da 106 | Muda invariante de imutabilidade legal no dia em que 5 dentistas estreiam |
| **Agrupar por caixa (px)**, não por tempo | Agrupar por tempo | O piso de altura infla consultas curtas: duas de 15min podem colidir na tela sem se cruzar no relógio |

## Desvios do plano original

| Item do plano | O que aconteceu | Impacto |
|---|---|---|
| Spec A0 §39: "Job A dogfood **antes** de refatorar o `FichasTab`" | Nunca houve dogfood; A0 refatorou e o redesign de 21/07 reescreveu por cima | **3 camadas de não-verificado empilhadas** — se quebrar, não dá pra saber qual causou |
| 106 destrava endo + implante + **perio** | Perio só ganhou a âncora | Declarado na spec e no roadmap; UI do periograma é frente própria |
| Roadmap A: "push no fim do roadmap" | Push aconteceu hoje | Nenhum — o roadmap fechou |

## Erros encontrados e como pensei em resolver

| Erro / problema | Causa | Como resolvi / pretendo resolver | Resolvido? |
|---|---|---|---|
| **Perda silenciosa de dado clínico** — ~55 dados ditados, ~8 guardados | `extractor: null` em endo/implante; perio inexistente | §3 do roadmap com ordem de correção; **aviso de perda é o item 1** | não |
| **Vocabulário divergiu em 3 camadas** — banco 17 tipos, TS 12, prompt 10 | A 106 ampliou o banco, TS e prompt ficaram | §3.1 da spec de eventos: enum do prompt **deriva** de `TIPOS_EMITIVEIS_PELA_IA` | não |
| **Agrupamento multi-dente morto** | Infra pronta (tag→uuid), zero instrução no prompt | Fase 2a; gate exige build de prod + Playwright | não |
| **Schema de endo nasce incompleto** | Dentista dita comprimento *aparente* **e** *real*; schema tem um só. Lima "35 reciprocante" não cabe em 8 chars | Corrigir **antes** do extractor, senão ele mira schema errado | não |
| `queixa_principal` semanticamente errada | Prompt pede "título do procedimento", campo significa queixa do paciente | Ajuste de prompt | não |
| **Agendamento recusado "sem nada no horário"** | Drawer fixava `dentistas[0]`, ignorando o filtro da tela → servidor checava agenda de OUTRO dentista | `dentistaPadraoForm()` segue o filtro, nos 2 fluxos e 3 caminhos de abertura | **sim** |
| **Janela de conflito em UTC** | `dia 00:00Z–23:59Z` = 21h do dia anterior às 21h → **agendamento após 21h não era checado** | Janela de véspera a dia seguinte; provado por query (21:30 vs 21:45 sobrepõem, janela antiga não via) | **sim** |
| **Sobreposição desenhada por cima** | Todo card com largura cheia da coluna | Extraído `layout-sobreposicao.ts`; 6 casos provados por script | **sim** |
| Conflito do dentista era beco sem saída | Sem override no Novo Agendamento | Aviso âmbar + "Marcar mesmo assim"; trava do paciente intocada **por construção** | **sim** |
| Tabela de endo divergiu do artefato | Form de edição não seguiu o canônico (o card de leitura seguiu) | Larguras fixas, rodapé inline, coral só em linha parcial — verificado ao vivo | **sim** |
| **Bloqueio de agenda vira paciente falso** | Não existe "compromisso" sem paciente | Dr. Renato cadastrado como paciente de si mesmo (0 fichas, sem telefone) | não |
| 9 specs concluídas na pasta ativa se dizendo pendentes | Campo `Status:` nunca atualizado | Arquivadas; dívida registrada: **não confie no cabeçalho da spec** | **sim** |
| **Tabela de migrations do Supabase tem buracos** | Só o que passa pelo MCP entra no registro | Não registra 097–103 (aplicadas). **Checar objeto no schema, nunca essa tabela** | sim (documentado) |

## Arquivos alterados

| Arquivo | Mudança |
|---|---|
| `supabase/migrations/…_106_detalhe_especialidade.sql` | **NOVO — APLICADA em prod** |
| `src/lib/especialidades/{endo,implante}.ts` | **NOVOS** — schemas Zod |
| `src/components/fichas/{endo,implante}-{card,form}.tsx` | **NOVOS** — tabela e campos |
| `src/components/fichas/endo-form.tsx` | Reescrito contra o artefato (larguras, rodapé, coral parcial) |
| `src/components/odontograma/ToothDetailPanel.tsx` | Form de especialidade + `sinalDoEvento` |
| `src/app/dashboard/agendamentos/actions.ts` | `janelaDeConflito()` + `forcarConflitoDentista` |
| `src/app/dashboard/agendamentos/_components/agendamentos-client.tsx` | `dentistaPadraoForm` (useCallback), duração livre, banner âmbar |
| `src/app/dashboard/agendamentos/_components/layout-sobreposicao.ts` | **NOVO** — algoritmo de faixas compartilhado |
| `src/app/dashboard/agendamentos/_components/{week,day}-view.tsx` | Semana sem scroll + as duas usam o layout compartilhado |
| `plans/roadmap/roadmap-mestre-2026-07-21.md` | **NOVO** — mapa único |
| `plans/specs/spec-eventos-ficha-organizacao.md` | **NOVO** — aguardando aprovação |
| `plans/concluidos/` | +3 roadmaps, +9 specs |

## O que ficou pra próxima sessão

1. **[CRÍTICO] Verificar compartilhamento com 2 contas** — 10 min. RLS no ar desde 18/07, nunca testada por login. 5 dentistas dividem paciente: se houver furo, todos descobrem juntos.
2. **[CRÍTICO] Aviso de perda de dado** (§3.1) — cada consulta ditada hoje que perde odontometria é prontuário que nunca mais terá aquilo.
3. **[ALTO] Push do `8168b01`** — a tabela de endo corrigida está só local.
4. **[ALTO] `queixa_principal`** (§3.2) e **schema de endo** (§3.3) — o schema **antes** do extractor.
5. **[ALTO] Vocabulário do prompt** (§3.8) — sem ele os 5 tipos da 106 seguem inalcançáveis.
6. **[MÉDIO] Extractors de endo e implante** por voz.
7. **[MÉDIO] "Compromisso" sem paciente** na agenda — hoje bloqueio vira paciente falso.
8. **[BAIXO] Apagar o paciente falso** "Renato Gonçalves Teixeira" — **só depois** de existir o substituto.

## O que eu estava planejando / cogitando

- **A meta "matar tudo do dentista esta semana" não fecha.** Periograma, cockpit/Job B e a próxima fase da ficha são frentes próprias. Blocos 1–3 da §0 são a semana realista; registrei a aritmética no roadmap para a decisão ser dele, com os números à vista.
- **A revisão por exceção é a peça de maior risco** de tudo que desenhamos. Falso positivo repetido queima a confiança dos 5 em uma semana — pior do que não ter. Não deve subir sem teste contra boca real.
- **O split de tela pode ter sido a resposta certa pro problema errado.** Com manual ≈ 0 e 5 tabelas largas de especialidade, uma faixa compacta no topo talvez sirva melhor — as tabelas é que precisam de largura. Decisão em aberto.
- **Job B destravou hoje** e ninguém percebeu: a regra de retomada da spec dizia "congela a §8 quando v3 A/B estiver em prod". Está. O próximo passo dela mudou de "esperar" para "reabrir → congelar → design-brief".
- **Financeiro tem uma armadilha**: a spec foi escrita contra prod **pré-099**. Hoje há 099 + 7 migrations. Reconferir as assunções antes de tocar a Frente 1 — senão executa contra um banco que não existe mais.
- **Sobre os artefatos:** eu leio o HTML, não vejo o renderizado (o browser pane trava no screenshot). Quando o Mateus manda print, o print vale mais que minha leitura — foi o print que revelou a coluna Referência engolindo a tela.
- **Método que se provou hoje:** medir estilo computado no mesmo tick do evento dá resultado **invertido** (React não re-renderizou). Quase reportei bug inexistente. Separar em chamadas distintas é o que torna a medição confiável.

## Como retomar
```bash
cd C:/Users/mateu/Desktop/Odonto.IA-main
git log --oneline -3          # 8168b01 (ficha) local; c4ff7bb (agenda) no ar
git log origin/main..HEAD     # o que falta subir

# o mapa único — comece pela §0 "A SEMANA"
#   plans/roadmap/roadmap-mestre-2026-07-21.md
```

## Dívidas técnicas registradas
- [ ] **Compartilhamento nunca testado por login** — o mais urgente
- [ ] **3 camadas de não-verificado no `FichasTab`** — premissa da spec A0 §39 violada
- [ ] **Vocabulário em 3 camadas divergentes** — banco 17 / TS 12 / prompt 10
- [ ] **`ponte` e `esfoliacao` proibidos por instrução** ([route.ts:298](../../src/app/api/dex/formatar-evolucao/route.ts)) — 2 das 8 especialidades não podem ser ditadas
- [ ] **Bloqueio de agenda = paciente falso** — vai acumular cadastro fantasma
- [ ] **Campo `Status:` de spec não é confiável** — confie no código e no banco
- [ ] **`supabase_migrations.schema_migrations` tem buracos** — cheque o objeto no schema
- [ ] `atualizarAgendamento` (reagendar) **não** ganhou o override do conflito — só `criar`
- [ ] `AssinaturaRecepcaoModal.tsx:34` — lint `set-state-in-effect` pré-existente
- [ ] Paciente de teste `Teste Design Review (apagar)` (`0875d28a-…`) não apagado
