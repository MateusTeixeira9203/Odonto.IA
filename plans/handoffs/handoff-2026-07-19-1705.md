# Handoff — 2026-07-19 17:05

## Plano / spec de referência
- **Plano ativo:** [`roadmap-3.1-2026-07-14.md`](../roadmap/roadmap-3.1-2026-07-14.md) — ▶ AGORA e fila #3/#4/#4b reescritos nesta sessão.
- **Specs desta sessão:** [`spec-modo-consulta-v3-odontograma.md`](../specs/spec-modo-consulta-v3-odontograma.md) (Fatia A + Fatia D endo) · [`DESIGN-odontograma-v3.md`](../specs/DESIGN-odontograma-v3.md) (design congelado).
- **Spec status:** Fatia A **implementada, commitada localmente e dogfooded — NÃO pushada/deployada**. Fatias B/C/D pendentes. Ficha v2 (#4b) = frente nova, spec a escrever.
- **Modo:** execução (port React do v3 Fatia A) → fechou em planejamento (sequenciamento Job A / Ficha v2, push adiado).

## O que trabalhamos
1. **Portei a Fatia A do Odontograma v3 pra React** — geometria compartilhada, `Odontograma` com camada de eventos + orientação de boca, `ToothDetailPanel`, lista agrupada, refactor da state machine do `consulta-client` (mata o flash preto), persistência dos eventos, fiscalização §1.10 (assinatura + PDF).
2. **Unifiquei o odontograma no Modo Consulta** (a pedido do Mateus) — o card de seleção v2 morreu; dente entra na ficha se tem evento revisado; aviso clicável pra citado-sem-evento (guard anti-alucinação preservado).
3. **5 commits organizados** (`b8740b0..742b1ee`) + memória atualizada.
4. **Gate parcial:** eval Motor A rodado (**14/16**), **dogfood E2E ao vivo PASS** (fluxo completo funcionou), 2 reviews de auditoria (TS+UX) disparados.
5. **Preview de design iterado até aprovação** (artifact `d5f66b1a`): dente anatômico, orientação de boca, canal-silhueta, endo com 1 medida, 13 seções por especialidade + interação.
6. **Fechamento (planejamento):** Job A perde a Fatia C → Ficha v2; push adiado até a ficha montar.

## O que concluímos
**PRONTO (código local, verificado por dogfood):**
- **Odontograma v3 Fatia A inteira** — Modo Consulta unificado, pintado, com painel de dente, fiscalização. Migrations 100/101/103 **em prod** (dry-run 74/74). Dogfood E2E confirmou: dita → boca nas 3 cores → painel cicla estado → salvou → eventos no banco com autoria/data → assinatura lista realizados+datas → PDF com CRO. typecheck/lint/build limpos.
- **Motor A** emitindo `odontograma_eventos` + `orto_manutencao`, validado por eval 14/16 contra Gemini real.

**Status geral:** **Parcial** — Fatia A completa e dogfooded, mas **não no ar** (push adiado de propósito). Fatias B/C/D não começaram. Ficha ainda usa odontograma antigo (esperado — é #4/#4b).

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| **Unificar o odontograma no Modo Consulta** (matar o card de seleção v2) | Manter os dois lado a lado | Pedido do Mateus; o pintado é mais completo. Dente entra na ficha por EVENTO revisado (painel = confirmação); sentinelas nos chips; citado-sem-evento vira aviso clicável (guard preservado) |
| **Orientação de boca no COMPONENTE** (superiores coroa pra baixo) | Só na tela da consulta | Correto — mesmo componente na consulta e na ficha; boca consistente em todo o sistema. ⚠️ mudou o visual de produção (antes raízes se encontravam no meio) |
| **Push/deploy ADIADO até a ficha montar** | Pushar a Fatia A agora | Subir agora = sistema meio-migrado (consulta nova, ficha antiga). Gate completo (eval→dogfood→auditoria→push) roda sobre o sistema consistente, no fim da Ficha v2 |
| **Job A perde a Fatia C → Ficha v2 (#4b)** | Manter Fatia C no Job A | Fatia C ("estado denso") já é redesenho da ficha — mesmo território da reformulação. Redesenhar UMA vez, com o quadro completo. Job A = A(feita)+B |
| **Ficha v2 = frente própria com spec própria** | Estender o Job A pra cobrir tudo | "Não é só o odontograma" (Mateus): + visualização da ficha salva + layout/organização. Merece spec dedicada |
| **Fail-soft nos eventos** (ficha salva mesmo se o insert dos eventos falhar) | Falhar tudo | Espírito D5: dado textual não se perde por falha da camada visual. Registrado como ponto a endurecer |
| Conta do eval → `qa-teste-dentista2@odontoia-test.local` | `test-diag-0712@` | A antiga foi limpa de prod; a QA (odontoia-test.local) segue viva |

## Desvios do plano original
| Item do plano | O que aconteceu | Impacto |
|---|---|---|
| Design "em validação de campo, componentes pausados" (handoff anterior) | Mateus aprovou o preview e mandou seguir; portei tudo na mesma sessão | Positivo — Fatia A saiu inteira |
| Roadmap: "gate = eval→dogfood→auditoria→push" pra fechar a Fatia A | Push adiado; auditoria mitigada pra quando a ficha montar | Muda sequenciamento: a Fatia A fica no código até a Ficha v2 fechar |
| Job A com Fatia C | Fatia C movida pra Ficha v2 | Escopo do Job A encolheu; Ficha v2 nasceu |

## Erros encontrados e como pensei em resolver

| Erro / problema | Causa | Como resolvi | Resolvido? |
|---|---|---|---|
| Eval falhou no login (timeout 15s) 2×| Conta `test-diag-0712@` foi limpa de prod; cold start do server | Troquei pra conta QA viva + timeout 30s | ✅ (14/16) |
| **Ficha (FichasTab) mostra odontograma ANTIGO / "vazio"** no form manual | O form manual nunca gerou eventos — só `dentes_afetados`+notas. Trocar o componente sem dar como criar eventos mostraria boca vazia (pior) | **Não é bug — é escopo do Job A/#4b.** O caminho por IA (campo mágico) já traz eventos via `formatar-evolucao`; o manual é a Ficha v2 | ✅ (por design, sequenciado) |
| **Eval: `negacao-canal-so-curativo` FALHOU 1×** | Numa das 3 rodadas o Gemini emitiu `endodontia` realizado apesar da negação ("não fiz o canal") | Hipótese: ajuste de prompt (reforçar regra 5 da §3.1.2). **Não corrigido** — é dívida, revisar quando mexer no prompt | ❌ (dívida) |
| **Eval: `profilaxia-boca-toda-99` instável** (98 vs 99 entre rodadas) | Ambiguidade "inferiores da frente" → às vezes sentinela 98 (arcada inf), às vezes 99 (boca toda) | Variação benigna de interpretação (admissível pela §7 da spec). **Não corrigido** | ❌ (tolerável) |

## Arquivos alterados
| Arquivo | Mudança |
|---|---|
| **Commit `b8740b0`** | migrations 100/101/103 + harness matriz (aplicadas em prod) |
| **Commit `8ef5fa3`** | Motor A: `formatar-evolucao`/`detectar-consulta` + types + eval |
| **Commit `1cbb75f`** | `tooth-geometry.ts` (novo) + `Odontograma.tsx` (reescrito) + `ToothDetailPanel.tsx` (novo) + globals.css |
| **Commit `7063cd7`** | `consulta-client.tsx` (fase única + unificação) + `tooth-group-list.tsx` (novo) + `consulta-assinatura-modal.tsx` + `actions.ts` + `page.tsx` + `prontuario-html.ts` + pdf route |
| **Commit `742b1ee`** | specs/roadmap/handoffs/CLAUDE.md acumulados |
| `roadmap-3.1` + eval (casos/runner/resultado) | ⏳ **não commitados** — vão no commit de fim desta sessão |

## O que ficou pra próxima sessão
1. **[PRIMEIRO] Coletar os 2 reviews de auditoria** — TS (`typescript-reviewer`) e UX (`ux-reviewer`) foram disparados e estavam rodando ao fim da sessão. Retomar via `SendMessage` pros ids (ver "Como retomar") OU checar os output files. Findings da Fatia A entram no esforço da ficha, não num push imediato.
2. **[ALTO] Job A — ficha rápida** (fila #4, spec aprovada), agora **sem a Fatia C**. Campo mágico + voz + anexo no FichasTab. A extração sai da state machine já refatorada.
3. **[ALTO] Ficha v2 — reformulação completa** (fila #4b) — escrever a spec: odontograma novo na ficha (manual + visualização da salva) + redesenho de layout/organização + absorve a Fatia C do Job A.
4. **[no fim] Gate completo + push** do odontograma+ficha juntos.
5. **[BAIXO] Fila #9 Financeiro** — prod-down ~2mo, independente, pode subir a qualquer hora.

## O que eu estava planejando / cogitando
- **Sequência Job A → Ficha v2:** o Job A entrega o "lado input" (ficha criada com IA já pinta), a Ficha v2 o "lado output/display" (visualização + organização). Separáveis, mas ambos tocam `FichasTab.tsx` — cuidado pra não redesenhar duas vezes (por isso a Fatia C foi pra #4b).
- **Endurecer o fail-soft dos eventos:** aviso não-bloqueante ("desenho não gravou, toque pra tentar") ou retry. Não fiz — muda o UX do salvar, quis a palavra do Mateus.
- **Os 2 evals falhos:** o de negação é o que mais me incomoda (dado clínico errado) — vale um ajuste de prompt reforçando "negação nunca vira realizado" antes de subir a Fatia A de vez.
- **Ficha v2 é a chance de repensar a organização inteira** — o Mateus quer isso explícito; não é só trocar componente. Vale um design-brief próprio (como fiz pro odontograma).

## Como retomar
```
1. Ler o ▶ AGORA do roadmap: plans/roadmap/roadmap-3.1-2026-07-14.md
2. Retomar os reviews de auditoria (rodavam ao fim da sessão):
   SendMessage to a0e612ef0e8bd2414 (typescript-reviewer) / af5a3b0acaacd078e (ux-reviewer)
   — ou checar os .output em tasks/. Findings → esforço da ficha.
3. Job A: começar pela Fatia B (campo mágico no FichasTab). Fatia A (data_atendimento) feita.
4. NÃO pushar até a Ficha v2 fechar (push = deploy de sistema meio-migrado).
5. Dogfood/eval: server prod (preview_start name=prod) + conta QA
   qa-teste-dentista2@odontoia-test.local / QaTeste2026! (Mateus loga; assistente não digita senha).
```

## Dívidas técnicas registradas
- [ ] **Eval `negacao-canal-so-curativo`** — Gemini emite `endodontia` realizado apesar da negação (1/3 rodadas). Ajuste de prompt (§3.1.2 regra 5) antes de subir a Fatia A.
- [ ] **Eval `profilaxia-boca-toda-99`** — instável 98↔99 (ambiguidade "inferiores da frente"). Benigno, tolerável.
- [ ] **Fail-soft dos eventos silencioso** — `salvarFichaConsulta` loga e segue se o insert dos eventos falhar; o dentista vê "salva". Endurecer na Ficha v2.
- [ ] **Ficha manual não gera eventos** — cosmético/esperado; é o núcleo do Job A/#4b.
- [ ] **2 evals abaixo do gate p95<6s** — latência méd=2932ms p95=6891ms (estourou o teto). Revisar `maxOutputTokens` do `formatar-evolucao` (schema maior com eventos) antes do push.
- [ ] **Reviews de auditoria não coletados** — findings pendentes (ver retomada #1).
- [ ] Herdadas: ledger de migrations em 096 (097–103 fora dele) · clínica QA + 3 contas a limpar · `atualizarAgendamento` sem conflito de dentista no servidor.
