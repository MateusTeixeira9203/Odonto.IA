# Handoff — 2026-07-20 01:42

## Plano / spec de referência
- **Plano ativo:** [`roadmap-3.1-2026-07-14.md`](../roadmap/roadmap-3.1-2026-07-14.md) — ▶ AGORA + Bloco 0/1 + fila #4 reescritos nesta sessão.
- **Specs desta sessão:** [`2026-07-16-job-a-ficha-rapida-spec.md`](../specs/2026-07-16-job-a-ficha-rapida-spec.md) (Fatia A + Fatia B executadas) · [`spec-modo-consulta-v3-odontograma.md`](../specs/spec-modo-consulta-v3-odontograma.md) (dívidas do Bloco 0 corrigidas).
- **Spec status:** Job A (fila #4) — Fatia A e Fatia B **codadas, typecheck/lint/build limpos, NADA commitado, NADA dogfoodado ao vivo**. Bloco 0 (dívidas da Fatia A do odontograma) **fechado de ponta a ponta**, inclusive migration aplicada em prod.
- **Modo:** execução direta (thread principal) — sem subagents de código, só os 2 reviewers de auditoria (typescript-reviewer + ux-reviewer) no meio do Bloco 0.

## O que trabalhamos
1. **Fechei o Bloco 0** (dívidas da Fatia A do odontograma, herdadas do handoff de 19/07): fix da negação no evento, investigação da latência, fail-soft com retry, e as duas auditorias (TS+UX) — que voltaram com BLOCK e cujos findings apliquei na mesma sessão.
2. **Apliquei a migration 104 em prod** (RLS reforçada + RPC atômica `regravar_odontograma_eventos`) — verificada por SQL direto.
3. **Commitei o Bloco 0** (`8f07ff2`).
4. **Executei o Job A inteiro** (fila #4) — e no caminho descobri que o registro anterior ("Fatia A feita") estava errado: só a migration 100 tinha rodado, nada do código. Fiz Fatia A (data do atendimento) e Fatia B (campo mágico) as duas.
5. **Tentei rodar design-review** no `CapturaLivreCard` novo — o screenshot do browser pane travou (bug já conhecido do projeto). Fiz uma auditoria por estilos computados via JS em vez disso, achei e corrigi um bug de contraste que eu mesmo tinha acabado de corrigir no Bloco 0 e reintroduzi no componente novo.

## O que concluímos

**PRONTO (código local, verificado por typecheck/lint/build — NÃO por navegador):**

- **Bloco 0 — fechado e commitado (`8f07ff2`):**
  - `negacao-canal-so-curativo` corrigido — bug era na camada visual (`odontograma_eventos` emitindo `{endodontia,realizado}` apesar da negação), o texto nunca quebrou. Fix = bloco forte inline na seção ODONTOGRAMA do prompt. **Confirmado 0/3 falhas em 3 rodadas de eval independentes.**
  - Latência p95 investigada — NÃO é `maxOutputTokens` (a saída é curta). Aceita como dívida por decisão sua.
  - Fail-soft endurecido: `salvarFichaConsulta` sinaliza `eventosFalharam`; nova action `regravarEventosOdontograma` faz o retry via RPC atômica.
  - **Migration 104 aplicada em prod e verificada**: a RLS de `odontograma_eventos` agora checa de verdade que a ficha não está assinada (antes só um comentário na tabela *dizia* isso); a RPC `regravar_odontograma_eventos` faz lock+delete+insert no mesmo statement (2 retries concorrentes não duplicam mais).
  - Auditoria UX: tokens `-ink` (coral/teal/slate/warning) pra texto sobre fundo tingido; 5 faces do dente ganharam teclado (eram 100% inoperáveis, WCAG 2.1.1); `role="dialog"` → `role="region"`; `aria-pressed` parou de mentir em modo clínico.

- **Job A — Fatia A (data do atendimento):**
  - Campo de data no form do `FichasTab` (default hoje, teto hoje, editável) + `procedimentos`/`conduta` agora salvam no form manual (antes só o modo consulta salvava).
  - 12 pontos de leitura trocados pra `data_atendimento` (PendenciasTab, timeline, contexto de IA, dashboard, prontuário/PDF, orçamentos, tratamento etc.) + 2 pontos de escrita explícitos (`salvarFichaConsulta`, `createFicha` legado).
  - **Bug achado no caminho**: vários lugares formatavam a data com `new Date('YYYY-MM-DD')`, que o JS trata como meia-noite UTC — em fuso BRT desloca um dia pra trás. Corrigido em todo lugar que exibia a data; extraído pra `lib/format-data-ficha.ts` (usado por `FichasTab` e `prontuario-html.ts`).
  - **Verificado no banco:** `0 nulos, 40/40 fichas coerentes` com o backfill da migration 100 (gate §10 da spec, conferido por SQL direto).

- **Job A — Fatia B (campo mágico):**
  - `useCapturaLivre` (novo hook, `src/hooks/useCapturaLivre.ts`) — extraído do `consulta-client.tsx` de forma mecânica (mover, não reescrever): voz, transcrição, detecção ao vivo. 102 linhas saíram do consulta-client, 16 entraram (a chamada do hook com alias pros nomes antigos) — resto do arquivo de 1300 linhas intocado.
  - `VoiceUX` movido de `consulta/_components/` pra `components/fichas/` (presentacional, só o import mudou).
  - `CapturaLivreCard` (novo, `components/fichas/captura-livre-card.tsx`) — relato (texto/voz/anexo) + chips de detecção + botão "Organizar com Dex" com loading progressivo. Não salva nada — só dispara o organizar e devolve o resultado pro `FichasTab`.
  - `/api/extrair-texto` (nova rota) + `lib/extract-text.ts` — parsers (mammoth/pdf-parse/TextDecoder/officeparser) extraídos de `processar-documento` (refactor extrativo, comportamento idêntico) e reusados pela rota nova. Sem persistência — processa e descarta.
  - Mapeamento IA→form no `FichasTab` (`aplicarEvolucaoDoOrganizar`): queixa, observação (+ alerta de alergia), dentes com notas, procedimentos, conduta. `origem='ficha_rapida'` só quando o preenchimento veio do Dex nesta edição (update nunca reescreve origem).

**Status geral:** **Parcial.** Código completo e limpo (typecheck/lint/build), mas **três verificações reais faltam**: dogfood ao vivo (login-gated), design-review completo (visual travado), e nada foi commitado ainda desta parte (Job A).

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| **Migration 104 aplicada em prod nesta sessão** (não só escrita) | Deixar pra aplicar no gate final da Ficha v2 | Você autorizou; é aditiva/RLS, sem ela a invariante #14 fica sem proteção real no retry que criei |
| **Commit do Bloco 0 fechado antes de abrir o Job A** | Acumular tudo num commit só no fim | Bloco 0 era unidade coerente e testada (2 auditorias fechadas); checkpoint antes de abrir frente nova |
| **Ordem Fatia A → Fatia B do Job A** (não pular pra B) | Fazer só o campo mágico, já que era o pedido mais visível | A spec (§4) é explícita: B depende de A (campo de data precisa existir no form antes do mapeamento da IA escrever nele) |
| **`aplicarEvolucaoDoOrganizar` ajusta `selectionMode` pra 'arch' quando há sentinela** | Fixar sempre em 'single' | Copiei o critério que `handleEdit` já usa (linha 666) — evita os botões de seleção ficarem incoerentes com o que a IA preencheu |
| **Confirmação antes de sobrescrever via `window.confirm` nativo** | Modal customizado (o projeto já tem `Dialog` do shadcn) | É uma checagem binária rara — ponytail: não vale o código extra de um modal pra isso |
| **Auditoria por estilos computados (JS) em vez de screenshot** | Insistir no screenshot travado, ou pular a verificação visual inteira | Screenshot trava (bug conhecido); estilos computados são objetivos pra contraste/tipografia, mesmo não substituindo pixel real |

## Desvios do plano original

| Item do plano | O que aconteceu na prática | Impacto |
|---|---|---|
| Roadmap dizia "Job A: agora só Fatias A (feita) + B" | **Fatia A NÃO estava feita** — só a migration 100 tinha rodado. Zero referência a `data_atendimento` em `src/` antes desta sessão. | Descoberto ao verificar antes de codar (regra: "código escrito ≠ verificado"). Corrigi a Fatia A inteira antes da B, como a spec sempre exigiu |
| "Bloco 1 — a ficha" no roadmap listava só "Job A" como passo 1.1, sem detalhar Fatia A/B | Executei as duas nesta sessão, mais do que o roadmap sugeria como próximo passo mínimo | Positivo — Job A saiu inteiro, mas aumenta a superfície não-testada que precisa de dogfood antes do gate |
| Push planejado só depois de Ficha v2 (Bloco 1.4) | Nada mudou aqui — só reforço: **push continua adiado**, e agora tem ainda mais código não verificado ao vivo acumulado (Bloco 0 + Job A inteiro) | Nenhum — decisão preexistente se mantém, só cresce a lista do que o gate final precisa cobrir |

## Erros encontrados e como pensei em resolver

| Erro / problema | Causa | Como resolvi | Resolvido? |
|---|---|---|---|
| `negacao-canal-so-curativo` falhava no eval | Camada visual do prompt (`odontograma_eventos`) não tinha a regra de negação reforçada — só um comentário fraco de referência cruzada | Bloco forte inline na seção ODONTOGRAMA, com exemplo negativo explícito | ✅ (0/3 em 3 rodadas) |
| `regravarEventosOdontograma` (código meu do Bloco 0) não respeitava ficha assinada | A invariante #14 só existia em comentário na migration 101 — a RLS nunca checou de verdade | Migration 104: RLS reforçada + RPC atômica com lock da ficha | ✅ (aplicada e verificada em prod) |
| Meu próprio card de fail-soft (Bloco 0) não tinha cor nenhuma | `--color-warning` nunca foi registrado no bloco `@theme` do Tailwind — `text-warning`/`bg-warning-pale` compilavam pra nada | Registrei `--color-warning` + os 4 tokens `-ink` no `@theme`, confirmei via grep no CSS compilado | ✅ |
| Datas retroativas exibindo o dia errado (achado durante a Fatia A) | `new Date('YYYY-MM-DD')` parseia como UTC meia-noite; em BRT (UTC-3) isso volta um dia | Formatação manual (split da string) em todo lugar que exibe `data_atendimento`; centralizado em `lib/format-data-ficha.ts` | ✅ |
| `CapturaLivreCard` (código desta sessão) reintroduziu `text-teal` como texto — o MESMO bug de contraste que corrigi no Bloco 0 | Não apliquei o próprio aprendizado do Bloco 0 ao escrever o componente novo logo em seguida | Troquei as 4 ocorrências pra `text-teal-ink`, rebuildei, confirmei via estilo computado | ✅ (mas só numericamente — não vi na tela) |
| Screenshot do browser pane trava em qualquer chamada (`screenshot` e `zoom`) | Bug já registrado na memória do projeto ("congela, async nunca comita") — provavelmente uma request/poll em background que nunca resolve, e o mecanismo de captura espera rede ociosa | Não resolvido — troquei pra auditoria via `javascript_tool` (estilos computados), que não depende de captura de bitmap | ❌ (contorno, não fix — fica pro Mateus testar ao vivo ou achar outro caminho) |

## Arquivos alterados

| Arquivo | Mudança |
|---|---|
| `supabase/migrations/20260719000000_104_odontograma_eventos_retry_atomico.sql` | NOVO — aplicado em prod nesta sessão |
| `src/app/api/dex/formatar-evolucao/route.ts` | Fix da negação (bloco forte na seção ODONTOGRAMA) |
| `src/app/consulta/[agendamentoId]/actions.ts` | `regravarEventosOdontograma` via RPC atômica; `data_atendimento` explícito no insert |
| `src/app/consulta/[agendamentoId]/_components/consulta-client.tsx` | Extração do `useCapturaLivre` (behavior-preserving) |
| `src/app/consulta/[agendamentoId]/_components/voice-ux.tsx` | **DELETADO** — movido pra `components/fichas/voice-ux.tsx` |
| `src/hooks/useCapturaLivre.ts` | NOVO — hook extraído |
| `src/components/fichas/voice-ux.tsx` | NOVO (movido, idêntico) |
| `src/components/fichas/captura-livre-card.tsx` | NOVO — o campo mágico |
| `src/app/api/extrair-texto/route.ts` | NOVO |
| `src/lib/extract-text.ts` | NOVO — parsers extraídos de `processar-documento` |
| `src/app/api/processar-documento/route.ts` | Refactor extrativo (consome `extract-text.ts`) |
| `src/lib/format-data-ficha.ts` | NOVO — formatação segura de data (evita bug de fuso) |
| `src/components/pacientes/FichasTab.tsx` | Campo de data + procedimentos/conduta + `CapturaLivreCard` + mapeamento IA→form |
| `src/components/pacientes/PendenciasTab.tsx` | `data_atendimento` no lugar de `created_at` |
| `src/app/globals.css` | Tokens `-ink` + `--color-warning` no `@theme` (Bloco 0) |
| `src/components/odontograma/Odontograma.tsx`, `ToothDetailPanel.tsx` | Fixes de acessibilidade (Bloco 0) |
| 12 arquivos de pontos de leitura da Fatia A | `dentista-dashboard.tsx`, `orcamentos-client.tsx`, `paciente-detail-client.tsx`, `types.ts`, `pacientes/[id]/actions.ts`, `tratamento-actions.ts`, `demo/page.tsx`, `ai/context.ts`, `prontuario-html.ts`, `prontuario/route.ts`, `fichas/[id]/pdf/route.ts`, `get-patient-workspace-data.ts`, `get-visible-timeline-events.ts`, `consulta/[agendamentoId]/page.tsx`, `fichas/nova/actions.ts` — todos trocando `created_at` por `data_atendimento` na semântica clínica |
| `plans/roadmap/roadmap-3.1-2026-07-14.md` | Reescrito nesta sessão |

## O que ficou pra próxima sessão

1. **[CRÍTICO] Dogfood ao vivo do Job A** — nenhum gate do §10 da spec foi clicado de verdade. Você testa amanhã: campo mágico (digitar/colar/voz/anexo), "Organizar com Dex", confirmação antes de sobrescrever, data retroativa ordenando certo, anexo de PDF escaneado mostrando o erro certo.
2. **[ALTO] `design-review` completo** — só fiz a parte de contraste/tipografia (números, não pixel). Hierarquia visual, "sensação premium", AI-slop ainda não foram vistos. Screenshot trava — ou você testa e me diz o que vê, ou achamos outro caminho de captura.
3. **[ALTO] Decidir o que fazer com os touch targets do `CapturaLivreCard`** (~28px, herdado do padrão já existente no `FichasTab`) — é decisão de design-system, não bug pontual.
4. **[MÉDIO] Commit** — nada do Job A foi commitado. Combine comigo quando quiser fechar.
5. **[BAIXO] Apagar o paciente de teste** (`Teste Design Review (apagar)`, clínica QA) quando não precisar mais dele.
6. **[BAIXO] Verificar `DocumentosTab`** depois do refactor de `processar-documento` — o comportamento deveria ser idêntico (refactor extrativo), mas nunca foi clicado.

## O que eu estava planejando / cogitando

- **A Ficha v2 (#4b) ainda não tem spec.** Job A entrega o "lado input" (campo mágico); Ficha v2 é o "lado output" (visualização + manual + redesenho) + absorve a Fatia C. Pensei em escrever essa spec logo, mas você quis parar pra testar primeiro — faz sentido, testar o Job A pode revelar que a Ficha v2 precisa reconsiderar algo do form que acabou de mudar (o form ganhou 3 campos novos: data, procedimentos, conduta).
- **A ordem B→D→C das fatias do odontograma (decidida 19/07) ainda não foi retomada** — o Job A cortou na frente por decisão sua em 19/07 ("Bloco 1 antes do Bloco 2"). Continua correta, só not started.
- **Cogitei propor rodar o `design-review` de novo via Playwright MCP** (separado do Claude_Browser) em vez de insistir no browser pane — não cheguei a tentar porque exigiria um login novo nesse outro browser, e login é bloqueado pra mim. Se o travamento persistir, vale investigar se há uma flag/config pra desligar o "esperar rede ociosa" do tool de screenshot.
- **O achado da Fatia A "não feita" me deixou mais cauteloso** com qualquer "X (feita)" que eu leia em handoff/roadmap antigo sem checar o código — pretendo continuar verificando por grep/SQL antes de assumir, mesmo quando o registro parecer confiável.

## Como retomar
```
1. Ler o ▶ AGORA do roadmap: plans/roadmap/roadmap-3.1-2026-07-14.md
2. Login: Dra. Dois QA Teste (Mateus loga; assistente não digita senha)
   → navegar até paciente "Teste Design Review (apagar)" (id 0875d28a-223d-4821-be33-8c37b79b234e)
   → aba Histórico Clínico → Nova Evolução → testar o CapturaLivreCard
3. Preview: preview_start name=prod (precisa rebuild se algo mudar — npm run build antes)
4. Gates a testar (spec Job A §10, Fatia B): ver lista no item [CRÍTICO] acima
5. Depois do dogfood: decidir sobre commit + touch targets + Ficha v2
```

## Dívidas técnicas registradas

- [ ] **Touch targets do `CapturaLivreCard`** (~28px) — herdado do `FichasTab`, decisão de design-system.
- [ ] **Paciente de teste não apagado** — `id=0875d28a-223d-4821-be33-8c37b79b234e`, clínica QA.
- [ ] **`design-review` incompleto** — só contraste/tipografia via estilo computado; hierarquia/AI-slop pendem de pixel real.
- [ ] **Gates §10 da spec Job A não clicados ao vivo** — tudo verificado por código/typecheck, zero clique real.
- [ ] **`DocumentosTab` não re-verificado** após o refactor extrativo de `processar-documento` (deveria ser idêntico, nunca testado).
- [ ] **Herdadas do Bloco 0/19-07:** `get_my_dentista_id()` sem escopo de clínica (pré-existente 089) · competição visual dos cards na tela "salvo" · latência p95 aceita como dívida · `rajadas-acumuladas` instável no eval (ruído, não regressão) · ledger de migrations em 096 (097–104 fora dele) · clínica QA + contas a limpar.
