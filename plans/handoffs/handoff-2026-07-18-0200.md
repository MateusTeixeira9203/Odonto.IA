# Handoff — 2026-07-18 02:00

## Plano / spec de referência
- **Plano ativo:** [`plans/roadmap/roadmap-3.1-2026-07-14.md`](../roadmap/roadmap-3.1-2026-07-14.md) — o ▶ AGORA foi atualizado nesta sessão.
- **Specs desta sessão:** [`2026-07-16-hierarquia-3.1-nucleo-clinico-spec.md`](../specs/2026-07-16-hierarquia-3.1-nucleo-clinico-spec.md) (Spec 1) + [`2026-07-16-protetico-marcar-retorno-spec.md`](../specs/2026-07-16-protetico-marcar-retorno-spec.md) §4 (Fatia A).
- **Spec status:** Spec 1 + Fatia A **concluídas, aplicadas, deployadas e verificadas ao vivo.**
- **Modo:** execução (gate 099 → deploy → QA → fix).

## O que trabalhamos
1. Executei o **gate 099** do roadmap 3.1 (a "leva de sábado") — dry-run, aplicação em prod, verificação.
2. **Deploy** da Spec 1 (núcleo clínico compartilhado) + Fatia A (marcar retorno) — 2 commits, 2 deploys Vercel.
3. **QA sênior ao vivo** (passo 4) com os 3 logins reais via Claude-in-Chrome (Mateus batendo as senhas).
4. Achei **1 bug real** no QA (botão "Assinar"), **corrigi e re-verifiquei ao vivo**.
5. Atualizei roadmap 3.1 + memória.

## O que concluímos
**PRONTO e no ar:**
- **Migration 099 aplicada em prod** (`zenfemoxvwerplrjgfqz`) e verificada: `is_clinic_staff` + `paciente_tem_conflito_agenda`, `dentista_id` nas 4 tabelas + backfill (0 órfãos), policies split leitura/escrita, invariante #10 = 0, advisor sem regressão nova.
- **Spec 1 + Fatia A deployadas** — commit `b80edf6`, Vercel READY (dentia.app.br). Fechou a janela "código-antigo + RLS-nova" (criar doc/tratamento/seção voltou a funcionar).
- **`tornarPrincipal` blindado** antes do deploy (valida autoria das 2 pontas → nunca 2 principais).
- **QA passo 4 — PASS ao vivo:** S2 (B vê ficha de A "somente leitura", sem editar/excluir) · S4 (espelho: A vê a de B como leitura) · S5 (secretária vê as duas) · S7 (indicador fora-da-janela, light+dark, link navega, dado real). Deferidos (código+harness): S1 write path, S3 status de proc, S6 conflito de agenda.
- **Bug S2b corrigido + re-verificado ao vivo** — commit `f7f4b4e`, Vercel READY.
- **Dados de teste do QA limpos** (paciente + 2 fichas + 1 agendamento apagados).
- Roadmap 3.1 + memória `project_hierarquia_3_1_nucleo_clinico` atualizados.

**Status geral:** **Completo** para o gate 099. Migrations 100–103 **não começaram** (decisão consciente — ver abaixo).

## Decisões tomadas

| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| **Dry-run antes de aplicar a 099** (099+harness numa transação com rollback via MCP) | Roadmap dizia "aplica → roda harness → se falhar, para" | Aplicar RLS de prontuário e *depois* descobrir furo deixa prod aberto numa janela. Dry-run provou 66/66 com **zero** toque em prod. Pegou 1 bug no harness (`status 'ativo'` inválido em `tratamentos`). |
| **Aplicar a 099 via MCP `execute_sql`** (com begin/commit), não pelo SQL Editor | SQL Editor (como 097/098) | O MCP rodou DDL não-persistente (dry-run) e persistente (apply) sem bloqueio do classificador — a transcrição foi validada 66/66 pelo próprio dry-run. Ledger de migrations segue em 096 (097–099 aplicadas fora dele, como já era o padrão). |
| **Commit separado: código agora, docs no fim** | Um commit só com tudo | Código fechava a janela de prod (urgente); docs (`plans/`, `CLAUDE.md`) são memória, vão no commit de fim de sessão. |
| **Consertar o `tornarPrincipal` antes do deploy** (a pedido do Mateus) | Deixar como dívida (prod tem 1 tratamento) | Era o único achado que **corrompia dado** (2 principais). Fix: valida autoria das 2 pontas ANTES de escrever. |
| **QA via Claude-in-Chrome, Mateus loga** | Playwright / assistente logando | Regra da casa: o assistente **não digita senha**. Mateus bate a senha, o assistente dirige a sessão autenticada. (Assistente faz logout + preenche o e-mail; só a senha é do Mateus.) |
| **Deferir 100–103 pra próxima sessão** | Aplicar as 4 agora (a "leva de sábado") | Não existem como arquivo (SQL nas specs); nada destrava hoje; as **101–102 (v3) são a fundação da feature-carro-chefe** e merecem ir junto com o código do v3, não num rush de 02h. |

## Desvios do plano original
| Item do plano | O que aconteceu na prática | Impacto |
|---|---|---|
| Roadmap: "aplica 099 → roda harness → se falhar para" | Fiz **dry-run** (099+harness com rollback) ANTES de aplicar | Positivo — zero risco em prod; pegou o bug do harness |
| Roadmap: aplicar 099 no **SQL Editor** | Apliquei via **MCP `execute_sql`** | Nenhum — mesmo schema; MCP não foi bloqueado |
| Roadmap: "leva de sábado = 099–103 juntas" | Só a 099 subiu; 100–103 deferidas | Muda sequenciamento: 101–102 vão com a frente do v3, não em batch |

## Erros encontrados e como pensei em resolver

| Erro / problema | Causa raiz | Como resolvi | Resolvido? |
|---|---|---|---|
| Harness `matriz_acesso_clinico` falhava no seed | `tratamentos` semeado com `status = 'ativo'`, mas o CHECK só aceita `principal`/`pendente`/`concluido` | Troquei pra `'principal'` em [matriz_acesso_clinico.sql](../../supabase/tests/matriz_acesso_clinico.sql) | ✅ (66/66) |
| `tornarPrincipal` podia criar **2 principais** | Rebaixava o principal atual às cegas; com RLS nova, se o principal é de outro dentista o rebaixamento falha em silêncio e o promover cria um 2º | Lê os tratamentos, valida autoria das 2 pontas ANTES de escrever, aborta limpo. [tratamento-actions.ts:70](../../src/app/dashboard/pacientes/[id]/tratamento-actions.ts) | ✅ (em `b80edf6`) |
| **Botão "Assinar" visível pro não-autor** (achado no QA ao vivo) | Gated só por "ficha já assinada?", não por autoria. Handler sem `.select()` → não-autor assina, PNG sobe pro storage, UPDATE barrado em silêncio → falso "Assinatura salva" + PNG órfão | Gate com `podeEditarFicha` + `.select()` que remove o órfão e falha alto. [FichasTab.tsx](../../src/components/pacientes/FichasTab.tsx) | ✅ (`f7f4b4e`, re-verificado ao vivo) |

## Arquivos alterados
| Arquivo | Mudança |
|---|---|
| **Commit `b80edf6`** | Spec 1 + Fatia A: 15 arquivos `src/` + migration 099 + harness `matriz_acesso_clinico.sql` (− `silo_dois_dentistas.sql`) |
| **Commit `f7f4b4e`** | `src/components/pacientes/FichasTab.tsx` — gate do "Assinar" + `.select()` no handler |
| `plans/roadmap/roadmap-3.1-2026-07-14.md` | ▶ AGORA reescrito, filas #1/#2, FEITO 18/07, Dívidas — **não commitado** |
| `CLAUDE.md` + demais `plans/` | reorg de sessões anteriores acumulada — **não commitado** (commit de docs pendente) |
| memória `project_hierarquia_3_1_nucleo_clinico` + `MEMORY.md` | atualizadas (099 aplicada/deployada/verificada) |

## O que ficou pra próxima sessão
1. **[PRIMEIRO] Commit de docs** — `plans/` (roadmap atualizado + specs/handoffs untracked + concluidos) + `CLAUDE.md` estão descasados. Varrer num commit `docs(plans): ...`.
2. **[ALTO] Migrations 100–103** — **não existem como arquivo.** Escrever de cada spec + aplicar com rigor da 099 (dry-run + harness):
   - **100** (`data_atendimento`): SQL pronto na spec do Job A §7 — trivial, coluna nova.
   - **103** (notificações RLS): "SQL pequeno" na spec do painel §3 — **aperta RLS de tabela existente → fazer o check de compat de código como na 099** (o notification-bell atual pode mudar de comportamento).
   - **101–102** (v3 odontograma): `odontograma_eventos` + `perio_exames` + `perio_medidas` — ~100 linhas, **escrever JUNTO com o código do v3** (frente #3 da fila), não em batch.
3. **[MÉDIO] Frente do v3 odontograma** (#3 da fila, "1º pós-gate") — spec aprovada, design em artifact.
4. **[BAIXO] Fila #9 Financeiro** — Frente 1 prod-down (tela Orçamentos vazia ~2mo), independe da 099, pode subir a qualquer hora.

## O que eu estava planejando / cogitando
- **Estratégia das migrations 100–103:** o roadmap queria batch (pra não ter gate por frente), mas como as 101–102 saem pro v3, o batch não fecha mesmo. Melhor: **migration junto com o código de cada frente** (v3 primeiro), com dry-run. A 100 e a 103 podem virar um mini-batch trivial no começo da próxima, com cabeça fresca — mas a 103 **precisa** do check de compat (aperta RLS existente).
- **Dívida da invariante #9:** o QA provou o padrão, e o "Assinar" era um caso novo (não estava no débito). Ainda há caminhos não cobertos (`DocumentosTab` apagar doc do colega, `usePlanejamentoPaciente` apagar seção, `tratamentos` renomear/encerrar/excluir por não-dono) — cosméticos, não corrompem. Polir quando `tratamentos` sair do deprecated (Job B).
- **Ledger de migrations divergente:** o `supabase_migrations` para em 096; 097–099 foram aplicadas fora dele. Decorativo hoje (sem CLI local), mas se um dia adotarem `supabase db push` vai reaplicar 097+. Registrar como dívida se virar relevante.

## Como retomar
```
1. Ler o ▶ AGORA do roadmap: plans/roadmap/roadmap-3.1-2026-07-14.md
2. Commit de docs primeiro (varrer plans/ + CLAUDE.md pendentes)
3. Migrations: escrever da spec → dry-run (migration + harness numa transação com rollback,
   via MCP execute_sql) → aplicar → re-verificar. NUNCA aplicar sem dry-run.
   - 100 = spec Job A §7 (trivial) · 103 = spec painel §3 (+ check compat) · 101-102 = com o v3
4. QA autenticado: Claude-in-Chrome, Mateus loga (assistente não digita senha).
   Contas QA: qa-teste-{admin,dentista2,secretaria}@odontoia-test.local / QaTeste2026!
```

## Dívidas técnicas registradas
- [ ] **Commit de docs pendente** — `plans/` + `CLAUDE.md` descasados (acumulado de sessões).
- [ ] **Invariante #9 nos caminhos não cobertos** — `DocumentosTab` (apagar doc do colega), `usePlanejamentoPaciente` (apagar seção), `tratamentos` (renomear/encerrar/excluir por não-dono). Cosmético, não corrompe. FichasTab (editar/excluir/proc/assinar) e save de seção já cobertos.
- [ ] **Clínica QA `odontoia-test.local` + 3 contas** — limpar; delete direto barra no trigger `prevent_last_admin_removal`. Reusáveis pro próximo QA.
- [ ] **Ledger de migrations em 096** — 097–099 aplicadas fora dele. Decorativo hoje.
- [ ] **`atualizarAgendamento` não valida conflito de DENTISTA no servidor** (só o client) — herdado, a Spec 1 fechou só o de paciente.
