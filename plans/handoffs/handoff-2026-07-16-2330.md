# Handoff — 2026-07-16 23:30

> **Sessão de PLANEJAMENTO pura — a maratona das specs.** Zero código de produto tocado,
> zero commits (`git log` no topo segue `ca1b4a4`; o working tree continua carregando o
> código não commitado da sessão de execução anterior, intacto). O que mudou foi `plans/`:
> **o roadmap 3.1 saiu de "2 specs aprovadas + 4 buracos" pra FILA INTEIRA ESPEÇADA E
> APROVADA** — 5 specs trabalhadas e aprovadas hoje, mais decisões do protético registradas.
> O único portão entre o papel e o código é o **sábado 18/07** (099 → harness → commit).

## Plano / spec de referência
- **Roadmap ativo:** `plans/roadmap/roadmap-3.1-2026-07-14.md` — fila renumerada (8 posições,
  todas com spec viva), checklist de sábado expandido (agora inclui migrations 100–103).
- **Specs desta sessão** (todas em `plans/specs/`):

| Spec | Status ao fim da sessão |
|---|---|
| `spec-modo-consulta-v3-odontograma.md` | **APROVADA** (descongelada + revisada pro 099 + validada por pesquisa) — 1ª execução pós-gate |
| `2026-07-16-job-a-ficha-rapida-spec.md` | **APROVADA** (escopo + design) — executa depois do v3 |
| `2026-07-16-transcricao-tratada-spec.md` | **APROVADA** — pós-v3, não simultânea com Job A |
| `2026-07-16-job-b-cockpit-tratamento-spec.md` | **APROVADA estrutural** ("spec 100%") — contratos §8 congelam pós-v3 A/B (regra de retomada escrita) |
| `2026-07-16-painel-dex-notificacoes-spec.md` | **APROVADA** — Fatia 0 (migration 103) entra no sábado |
| Spec 3 §5 (protético, Fatia B) | Spec final segue **pós-099** (decisão da manhã mantida); decisões novas registradas no roadmap + painel §6 |

## O que trabalhamos
1. **Job A (ficha rápida)** — spec escrita e aprovada: campo mágico no form existente do
   perfil (form = tela de confirmação), voz completa extraída da consulta (`useCapturaLivre`),
   anexos (áudio → `/api/transcrever`; pdf/docx/txt → rota nova `extrair-texto`; foto/OCR
   fora), `data_atendimento` retroativa (migration 100), Fatia C = estado denso (gate de
   densidade + design-review). Design aprovado em mockup.
2. **Transcrição tratada** — spec escrita e aprovada: relato da consulta tratado por IA →
   `fichas.transcricao` (coluna virgem, **zero migration**), aberta pra clínica, gerada no
   SAVE (fail-soft), imutável, seção colapsável no fim da expansão da ficha + PDF.
3. **Odontograma v3 DESCONGELADO** — decisão do Mateus ("nós vamos mexer aqui"). Auditoria
   achou RLS do silo morto na spec (corrigida pro modelo 099 — argumento clínico: o acumulado
   com silo faria a boca MENTIR entre dentistas). **Pesquisa com fonte** (Manual do Prontuário
   CFO 2026, SDCEP, AAP/EFP 2018, 23 especialidades) validou o desenho ponto a ponto e rendeu:
   seção "Cobertura por especialidade", perio com recessão POR SÍTIO + CAL derivado + supuração,
   catálogo 12 tipos (+`fratura`, +`pino_nucleo`), rótulo Incisal, orto com elásticos
   corrente/intermaxilar + atalho "igual à última". Checklist de requisitos por especialidade
   do Mateus respondido item a item (✅/🟡/❌ com não-escopos justificados). **Design de
   referência publicado como ARTIFACT** (3 versões, mesma URL):
   `https://claude.ai/code/artifact/2cea5588-2f4a-4ba5-9330-6d69ac17d965`
4. **Job B (cockpit)** — investigação (tratamentos 1 linha/0 vínculos em 33; PendenciasTab é
   o embrião), spec estrutural escrita, e **REFRAME do Mateus no mesmo dia**: *"o cockpit
   será o novo modo consulta"* — mapa de tratamento central + radiografia lado a lado +
   captura em 1 linha. Spec reescrita nessa forma e aprovada. Dívida das tabelas mortas
   FECHADA com decisão.
5. **Painel do Dex** — investigação matou o mistério dos 0/48: **3 camadas provadas**
   (donos-admin fora do filtro de role; mobile SEM sino; abrir = marca tudo lido) + vazamento
   de RLS confirmado. Spec escrita e aprovada com: mapa canônico de notificações (10 produtores
   auditados no código), migration 103 (SQL pronto na spec), design do painel do Dex
   (agrupamento temporal + 3 famílias + bottom sheet mobile), e fonte nova **"pacientes sem
   retorno 30/60"** (alerta COMPUTADO, regra híbrida de atribuição, dono vê 2 linhas).
6. **Protético** — decisões registradas: vê primeiro nome + trabalho + prazo (minimização);
   ordem painel→protético; spec final pós-099.

## O que concluímos
**Status geral: COMPLETO no que a sessão se propôs** (planejamento). Nada de código, nada
aplicado, nada testado — por desenho, não por falta.

✅ 5 specs aprovadas + decisões do protético + roadmap como mapa fiel + design de referência
do v3 em artifact + memória de pesquisa salva (`reference_prontuario_cfo_perio.md`).
✅ Roteiro da clínica piloto ganhou o **Bloco 8 — vocabulário de campo** (observar os termos
exatos que os dentistas falam; pergunta "que variável você anotaria SEMPRE se não custasse tempo").
❌ O gate de sábado continua 100% em aberto (099/harness/commit — herdado da sessão anterior).

## Decisões tomadas
| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| **v3 descongelado e 1º da fila pós-gate** (regra "trabalho só com isso") | Quick wins primeiro | Regra de congelamento do Mateus + ordem técnica: Job A extrai captura da state machine já refatorada; Fatia C reusa o tooth-group-list |
| **RLS do v3 → modelo 099** (`is_clinic_staff()` no SELECT das 3 tabelas novas) | Manter silo da spec original | Além de acompanhar a 099: o acumulado reduz eventos de TODOS — com silo, a boca renderizada mentiria (canal do A sumiria pro B) |
| Job A: campo mágico no form existente · `data_atendimento` coluna própria (`date`) · VoiceUX completa (escolha do Mateus contra minha recomendação de ditado simples) | Fluxo separado · created_at retroativo · ditado simples | Form = tela de confirmação (menor código); created_at retroativo falsifica auditoria de prontuário; Mateus: mesma experiência de voz em todo o produto |
| Job A anexos: áudio + pdf/docx/txt; **foto/OCR fora** | OCR por visão no v1 | "Transcrito" literal = áudio; parsers de texto já existem; OCR é escopo de IA novo → fatia futura própria |
| Transcrição tratada: **aberta pra clínica** (coluna existente) · gera no SAVE · fail-soft · imutável · tratar ≠ resumir | Tabela privada · gerar no organizar · bloquear save · editável | Coerente com 3.1 + zero migration; trata a versão final 1×; ficha nunca refém do anexo; a ficha resume, a transcrição documenta |
| v3 catálogo: +`fratura` +`pino_nucleo` (pino = kit do clínico geral, por decisão do Mateus) | Mapear só por observação | Fratura é condição padrão de odontograma (fonte); pino aparece nos dados reais dele (dente 13) |
| Perio: recessão POR SÍTIO + CAL derivado (nunca persistido) + supuração (reentrou por pedido) | 1 recessão por dente (spec original) | Estadiamento AAP/EFP 2018 é dirigido por CAL interdental — sem isso "periodontograma completo" não estadia |
| **Job B = o novo modo consulta** (reframe do Mateus): mapa central, radiografia lado a lado (reverte o corte "PiP depois" de 14/07), captura em 1 linha | Cockpit só como aba de leitura no perfil (1ª versão da spec, superada no mesmo dia) | "Da ótica do dentista durante a consulta": o que ele CONSULTA fica grande, o que ele PRODUZ (transcrição automática) fica pequeno |
| Job B: derivado SEM container (`tratamentos` deprecated) · substitui PendenciasTab · fonte híbrida (eventos + legado) · zero LLM | Reviver container manual · conviver com PendenciasTab · só eventos | Dado de prod (1/0/33) + campo ("mal têm tempo"); 2 lugares divergem; 33 fichas atuais não têm eventos (cockpit nasceria vazio) |
| Painel: **destinatário é a PESSOA** (role só broadcast) · **lida por item** (abrir não zera) · migration 103 no SÁBADO · sino vira PAINEL DO DEX (temporal: Hoje/Ontem/Antes · 3 famílias · bottom sheet mobile · sem página nova) | Role primário · abrir=zerar · esperar spec · dropdown genérico | Dono-admin atende paciente; badge tem que dizer a verdade; furo vivo + destrava protético; "o que aconteceu hoje?" é a pergunta real do dentista |
| **Sem retorno 30/60 = alerta COMPUTADO** com atribuição **HÍBRIDA** (pendência aberta → quem indicou; senão → último atendimento) · dono vê 2 linhas (dele + clínica) | Notificação por evento · regra só-último-atendimento | Estado contínuo ≠ evento (zero cron/dedup); a regra única roteava errado o caso do canal (apareceria pro endodontista que já terminou, não pro clínico que devia chamar) |
| Protético vê **primeiro nome + trabalho + prazo** | Nome completo | Minimização LGPD — papel externo à cadeia clínica |
| Specs "estruturais" com contratos PINADOS (Job B §8) | Congelar contrato sobre fundação não executada | A lição de 16/07 de manhã, aplicada 2× hoje — o v3 mesmo provou o custo (spec de 13/07 já nasceu com RLS morta) |

## Desvios do plano original
| Item | O que aconteceu | Impacto |
|---|---|---|
| Handoff anterior: "painel do Dex é o que eu puxaria primeiro" das specs livres | Mateus dirigiu a ordem: Job A → transcrição → v3 (descongelou) → Job B → painel | Nenhum — todas saíram, e o painel saiu por último COM o diagnóstico completo |
| Plano da manhã: "Fatia B do protético não se escreve antes da 099" | Mantido — mas as DECISÕES dela foram tomadas (primeiro nome, ordem painel→protético) | Sábado escreve a spec final com metade do trabalho pronto |
| "Sino melhorado, sem página nova" (decisão declarada na 1ª versão do painel) | Mateus evoluiu: sino vira o PAINEL DO DEX (continua overlay, ganha identidade + organização temporal) | Registrado na §3.1 — overlay mantido, ambição visual subiu |
| Job B v1 (perfil + briefing) aprovada às ~21h | Reframe do Mateus ("é o novo modo consulta") → spec REESCRITA na mesma noite | Zero re-trabalho de código (era estrutural); a moldura mudou, motor/invariantes ficaram |

## Erros encontrados e como pensei em resolver
| Erro / problema | Causa | Como resolvi / pensei em resolver | Resolvido? |
|---|---|---|---|
| 🐛 **0/48 notificações de dentista lidas** (mistério do handoff anterior) | **3 camadas somadas:** (1) donos têm role `admin` e as notificações nascem `para_role='dentista'` → filtro da rota nunca casa (provado: 8 endereçadas a admins ativos, invisíveis); (2) `NotificationBell` só existe em sidebar/dock DESKTOP — `MobileHeader`/`MobileDrawer` não têm sino; (3) `handleBellClick` marca TUDO como lido ao abrir | Spec do painel: destinatário-pessoa (rota + RLS com o MESMO shape), sino no mobile, lida por item. A 103 ainda marca como lidas as >30 dias pra não inundar com backlog | **Spec aprovada** — executa pós-gate |
| 🐛 **RLS de `notificacoes` ignora `para_dentista_id`** (herdado) | Policy filtra só role — B vê a do A por acesso direto/**realtime** (o sino assina INSERTs sem filtro) | **Migration 103 com SQL pronto na spec** (§3 Fatia 0) + 2 asserções pro harness — entra na leva de SÁBADO | Escrita, não aplicada |
| ⚠️ **Spec do v3 prescrevia o silo morto** (`is_own_clinical_record` no SELECT) | Escrita em 13/07, 3 dias ANTES da decisão do núcleo clínico | Revisada pro 099 em 7 pontos + nota de revisão. Lição: **spec escrita sobre modelo que mudou precisa de re-auditoria explícita antes de aprovar** | Sim |
| ⚠️ **"8 especialidades do CFO" era enquadramento falso** (são 23) | Número veio de memória, não de fonte | Pesquisa (research skill) → seção "Cobertura por especialidade" com claim honesto: cobre o núcleo dente-registrável | Sim |
| ⚠️ **Perio da spec não sustentava estadiamento** (recessão 1/dente → sem CAL por sítio) | Spec anterior à validação clínica | SDCEP + AAP/EFP 2018 → recessão por sítio (opcional, toque) + CAL derivado; schema ainda não aplicado = mudança grátis | Sim |
| 🤔 **Rajada de 10 `pagamento_confirmado` no mesmo dia (15/07)** | Suspeita: 3 produtores no código (financeiro + orçamentos ×2, da feature de 15/07) — possível "N parcelas = N notificações" | Item de investigação na execução do painel com gate "1 evento = 1 notificação" | **Não** — registrado |
| Resposta contraditória no seletor (fratura + pino + "nenhuma") | multiSelect permitiu as 3 | Interpretei pelo sinal claro (as 2 adições nomeadas), avisei e segui — Mateus confirmou depois ("núcleo intra canal pro clínico geral") | Sim |
| PDF do manual CFO ilegível pelas ferramentas (sem poppler na máquina) | `pdftoppm` não instalado | **Truque que fica:** `pdf-parse` do node_modules DO PRÓPRIO PROJETO extrai o texto (109 páginas ok) | Sim |
| Browser pane: screenshot timeout + `file://` negado; Mateus rejeitou retry | Pane instável (bate com a memória `qa-playwright-harness`) | Parei de dirigir o browser; verificação visual ficou com o próprio Mateus no artifact | Contornado |

## Arquivos alterados (sessão = só `plans/` + memória + artifact)
| Arquivo | Mudança |
|---|---|
| `plans/specs/2026-07-16-job-a-ficha-rapida-spec.md` | **NOVA** — aprovada (escopo + design) |
| `plans/specs/2026-07-16-transcricao-tratada-spec.md` | **NOVA** — aprovada |
| `plans/specs/2026-07-16-job-b-cockpit-tratamento-spec.md` | **NOVA** — reescrita 1× (reframe) — aprovada estrutural |
| `plans/specs/2026-07-16-painel-dex-notificacoes-spec.md` | **NOVA** — aprovada (inclui SQL da 103 e mapa canônico) |
| `plans/specs/spec-modo-consulta-v3-odontograma.md` | **REVISADA E APROVADA** — 099 + pesquisa + cobertura + catálogo 12 + perio + orto + fontes |
| `plans/specs/perguntas-clinica-piloto-odontograma-2026-07-13.md` | + Bloco 8 (vocabulário de campo) |
| `plans/roadmap/roadmap-3.1-2026-07-14.md` | Fila renumerada (8 posições, todas 🟢/🟡 com spec) · sábado ganhou passo 5 (migrations 100–103) · dívida "tabelas mortas" fechada |
| `memory/reference_prontuario_cfo_perio.md` + `MEMORY.md` | **NOVA** — fontes CFO/SDCEP/AAP-EFP pra reuso |
| Artifact `2cea5588…` (claude.ai) | Design de referência do v3 — 3 publicações, mesma URL |
| `src/**` e `supabase/**` | **INTOCADOS** — o working tree segue exatamente como a sessão de execução deixou |

## O que ficou pra próxima sessão
1. **[CRÍTICO] SÁBADO 18/07 — o gate.** Na ordem: (1) Mateus aplica a **099** no SQL Editor
   do DentAi (`zenfemoxvwerplrjgfqz`); (2) rodar `supabase/tests/matriz_acesso_clinico.sql`
   — reprova = para tudo; (3) **commit + push** Spec 1 + Fatia A; (4) teste com logins reais;
   (5) aplicar **100–103** na sequência — ⚠️ **os ARQUIVOS de migration 100–103 ainda não
   existem**: o SQL mora nas specs (Job A §7, v3 §1.7/§1.8, painel §3) e os arquivos nascem
   na sessão de sábado, na ordem real, com as +2 asserções da 103 no harness; (6) escrever a
   spec final da Fatia B do protético.
2. **[ALTO] Execução pós-gate, na fila:** v3 (A→B→C, dedicado — começa pelo `design-brief`
   da Parte 9, Opus) → Job A (A→B→C) → transcrição tratada → Job B (contratos congelam no
   meio) → painel Fatias 1–2 (encaixa em qualquer janela curta — arquivos disjuntos) →
   protético.
3. **[MÉDIO] Mateus em campo:** Bloco 8 do roteiro piloto — anotar o vocabulário falado
   (insumo do glossário) e a pergunta "que variável você anotaria sempre se não custasse tempo".
4. **[BAIXO] Herdados intactos:** `atualizarAgendamento` sem conflito de dentista server-side ·
   varrer `fichas.conduta` (2/35, cheiro de feature morta) · Redis offline · HIBP desligado ·
   24 eslint · 59 casts · `openai` órfão · bump next · contas de teste em prod.

## O que eu estava planejando / cogitando
- **Sábado vai ser pesado e merece roteiro:** 5 migrations + harness + commit + testes + spec
  do protético. Eu abriria a sessão criando os arquivos 100–103 a partir das specs (numeração
  limpa na ordem real de aplicação), aplicaria UMA POR VEZ com o harness entre 099 e o resto,
  e só depois a spec da Fatia B. Se o harness reprovar na 099, nada mais se aplica.
- **O design-brief do v3 é a primeira ação de execução** (Parte 9: ToothDetailPanel, lista
  agrupada, PerioGrade) — e o artifact de hoje é o insumo dele, não o substituto.
- **Cross-spec a vigiar:** Job A Fatia C pode reusar o `tooth-group-list` do v3 · a ficha
  rápida NÃO pinta o acumulado (sem eventos — se o Mateus quiser histórico migrado pintado,
  é extensão futura das duas specs) · transcrição + Job A tocam os mesmos arquivos da
  consulta DEPOIS do refactor do v3 — "uma frente por vez" resolve, mas quem entrar segundo
  re-verifica.
- **A rajada 10× de `pagamento_confirmado`** — se confirmar "N parcelas = N notificações",
  o fix é agregar por orçamento (1 notificação com soma), não suprimir.
- **O painel pode andar antes do v3 se sobrar uma janela curta** — arquivos disjuntos; eu
  levantaria isso com o Mateus quando a 099 estiver verde, porque o protético (que ele quer)
  destrava com o painel + 103.
- **Billing:** decidido por ele, traz "quando estiver 100%" — **não perguntar**.
- **Obsidian:** não capturei no vault — as decisões estão integralmente nas 5 specs + este
  handoff (fonte da verdade no repo). Se o Mateus quiser o espelho no brain_m, é um comando.

## Como retomar
```bash
cd "C:/Users/mateu/Desktop/Odonto.IA-main"
git status --short   # ~36 entradas: código da sessão de execução (intacto) + 5 specs novas
git log --oneline -1 # ca1b4a4 — ZERO commits desde 15/07 (planejamento não commita)
cat plans/roadmap/roadmap-3.1-2026-07-14.md  # a fila inteira, 8 posições, todas com spec
```
**Se for sábado:** seguir o checklist "▶ AGORA" do roadmap (099 → harness → commit → testes
→ 100–103 → spec protético). **Se for sessão de execução:** v3 Fatia A, começando pelo
design-brief. **Antes de qualquer código:** reler a spec da frente + a §7/§8 pinada se for
Job B.

## Dívidas técnicas registradas
- [ ] **Gate de sábado inteiro em aberto** (herdado): 099 + harness nunca rodaram; código da
      Spec 1/Fatia A escrito e não verificado; app quebra em runtime sem a 099.
- [ ] **Arquivos de migration 100–103 não existem** — SQL nas specs; criar na sessão de sábado.
- [ ] **RLS de `notificacoes` vazando** até a 103 (35+ notificações entre dentistas).
- [ ] **Realtime do sino assina INSERTs sem filtro** — pós-103 a RLS segura; gate específico na spec do painel.
- [ ] Rajada `pagamento_confirmado` 10×/15-07 — investigar na execução do painel.
- [ ] `checkin_paciente` tem produtor e 0 usos — fluxo de check-in não é usado (dado pro painel).
- [ ] Herdadas da sessão anterior: conflito de dentista server-side em `atualizarAgendamento` ·
      `fichas.conduta` 2/35 · tipos duplicados · Redis · HIBP · eslint 24 · casts 59 ·
      `openai` órfão · next 16.x · contas de teste em prod.
