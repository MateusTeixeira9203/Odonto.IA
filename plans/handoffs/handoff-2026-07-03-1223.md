# Handoff — 2026-07-03 12:23

## Plano / spec de referência
- **Roadmap ativo:** `plans/roadmap/roadmap-polimento.md` (16 itens do fundador + sobras da Fase 1 + Parte 2).
- **Specs desta sessão:** `spec-workflow-sessoes.md` (fluxo de sessões), `spec-odontograma-referencia.md` (odontograma no consulta), `spec-orcamento-cluster.md` (#13/#14/#15). **Todas aprovadas.**
- **Modo desta sessão:** planejamento (com execução residual no começo, antes de criarmos a regra 6).

## O que trabalhamos
1. **Deploy da Fase 1 em prod** (dentia.app.br, commit `7ba634e`) — K+L + correções; verifiquei migrations 079/080/081 aplicadas antes de subir.
2. **Reset da conta do Dr. Clênio** (`clenio21@gmail.com`) — zerei todos os dados (cascade), mantive o login → cai no onboarding do zero.
3. **Setup change (regra 6):** separei sessões em **planejamento vs execução** (CLAUDE.md + skills `session-start`/`handoff` + `spec-workflow-sessoes.md`).
4. **Planejei o cluster de orçamento** (#13 valor, #14 status, #15 scroll) → spec consolidado, decisões travadas.
5. **Codei (NÃO commitado):** consulta multi-proc por dente + prompt (limpeza/PPR/boca toda); odontograma de referência no consulta (decíduos); #12 novo-paciente (barra ancorada + toast).
6. **Config:** corrigi a **Site URL do Supabase** (#2 senha) e a **chave do Groq** (você atualizou a env do Vercel).

## O que concluímos
**Status geral: Parcial — planejamento avançado + código execução-pendente.**
- ✅ Deploy Fase 1 no ar (`7ba634e`) · ✅ Clênio resetado · ✅ Regra 6 · ✅ Spec do cluster orçamento · ✅ #2 Site URL corrigida.
- ⏳ Código de hoje (consulta/odontograma/#12): escrito, **`tsc`/`eslint` limpos**, **não testado ao vivo, não commitado**.

## Decisões tomadas
| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| Modos de sessão: planejamento vs execução (regra 6) | continuar misturando | evitar planejar+codar meio a meio (atrito nº1) |
| #14 status: 2 dimensões (comercial vs pagamento **derivado**) | 'pago' como status manual | pagamento já vive em `pagamentos` (selo Quitado); 'pago' órfão duplicava |
| #13 valor: input decimal **parse-no-blur** | calculadora-centavos corrigida | bate com o mental model do dentista ("250" = R$250) |
| Odontograma de referência no consulta (decíduos) | estender o Mini | um componente canônico, sem drift; decíduos de graça |
| Reset Clênio: manter login + zerar dados | apagar auth / re-signup | menos atrito; estado canônico de onboarding |

## Desvios do plano
- A sessão **começou em execução** (correções + deploy + reset Clênio + Groq) e **virou planejamento** no meio, quando criamos a regra 6. Por isso há código não-commitado numa sessão "de planejamento" — é a transição pro novo fluxo. A partir de agora os modos não se misturam.

## Erros encontrados e como resolvi
| Erro | Causa | Resolução | Resolvido? |
|---|---|---|---|
| Groq 401 em prod | `GROQ_API_KEY` antiga no Vercel | você atualizou a env; deploy 23:40 já respondia 200 | sim (parece) |
| #2 reset senha → 404 | Site URL do Supabase no domínio morto `dent-ia-gamma.vercel.app` | corrigida pra `https://dentia.app.br` | sim (falta re-testar com link NOVO) |
| #14 status pago/recusado não vai | constraint sem 'pago' + `handleStatusChange` engole o erro (sem toast) | Opção A (spec cluster) | especificado, não codado |
| #13 valor corrompe ao editar (250→289, zero grudado) | `value=formatCents` + onChange `replace(/\D/g,'')` (re-parseia string formatada) | Opção B (spec cluster) | especificado, não codado |

## Arquivos alterados (working tree — não commitado)
| Arquivo | Mudança |
|---|---|
| `CLAUDE.md` | regra 6 (modos de sessão) |
| `formatar-evolucao/route.ts` | prompt: arcada/boca inteira, limpeza→99, PPR sup→97/inf→98 |
| `consulta-client.tsx` | multi-proc por dente + swap p/ Odontograma + `toggleTooth` |
| `Odontograma.tsx` | prop `detectedTeeth` + estado amber + contador de aba |
| `arch-chips.tsx` (novo) · `mini-odontograma.tsx` (**deletado**) | chips de arcada extraídos; Mini aposentado |
| `novo-paciente-form.tsx` | barra de ações ancorada (blur+elevação) + toast de sucesso |
| `plans/roadmap/roadmap-polimento.md` (novo) | roadmap de polimento |
| `plans/specs/spec-workflow-sessoes.md`, `spec-odontograma-referencia.md`, `spec-orcamento-cluster.md` (novos) | contratos |
| **Local (gitignored, `.claude/`):** skills `session-start` + `handoff` | regra 6 nos rituais |

## O que ficou pra próxima sessão
### EXECUÇÃO pendente (backlog — quando rodar uma sessão de execução)
1. **[ALTO] Testar ao vivo + commitar + deployar o código de hoje** — consulta multi-proc (ditar "PPR inferior; canal/pino/provisório/coroa 13; extração 38/16"), odontograma decíduos (marcar dente 54), #12 (barra + toast). Ver `spec-odontograma-referencia.md`.
2. **[ALTO] Cluster de orçamento** — implementar `spec-orcamento-cluster.md` (#13/#14/#15).
3. **[MÉDIO] #2 fallback** — `verifyOtp` na `redefinir-senha` SE o link novo cair em "Link Expirado".
### PLANEJAMENTO (próxima sessão — continuar escopando ponto a ponto)
4. **#16** (ficha→ficha de tratamento), **#3** (recompensa), **#5** (demo), **#8** (DEX+motion), **#9** (perf), **transversal do feedback**.
5. Grandes: **C/D** (réguas de e-mail + relatório — Vercel Cron destravado), **F3** (design-review + polish, inclui polish da apresentação do #15), **#1** onboarding (**por último**, decisão do fundador).
### LIMPEZA
6. Contas `test-*-0630@` na prod.

## O que eu estava cogitando
- O código de hoje precisa de uma **sessão de execução antes do teste com a clínica grande** — não deixar acumular demais no working tree. Provável: 1 commit de docs (regra 6 + specs/roadmap) + commits por bloco (consulta/odontograma, #12) — a critério na execução.
- **#15 scroll do novo orçamento é empírico** — precisa reproduzir no browser (o da apresentação tem lead concreto: unificar o container de scroll dos slides).
- A regra 6 está no `CLAUDE.md` (vai pro repo), mas os **skills estão gitignored** (`.claude/`) — a máquina que executar precisa ter os skills locais. Ok, só lembrar.
- Sobre o Clênio: os arquivos de Storage dele ficaram órfãos (inofensivos) — limpar só se quiser impecável.

## Como retomar
```bash
cd "C:/Users/mateu/Desktop/Odonto.IA-main"
git status   # working tree: código de hoje + docs (não commitado); main = 7ba634e (deployado)
# PRÓXIMA = PLANEJAMENTO: ler plans/roadmap/roadmap-polimento.md e continuar de #16
# QUANDO FOR EXECUTAR: ler spec-orcamento-cluster.md + spec-odontograma-referencia.md → testar/commitar/deployar
```

## Dívidas técnicas registradas
- [ ] Working tree por commitar (código de hoje + docs). `main` = `7ba634e` já deployado.
- [ ] `set-state-in-effect` pré-existentes (`consulta-assinatura-modal.tsx`, `useDexGuide.ts`, `primeiros-passos-card.tsx`) — dívida antiga.
- [ ] Contas `test-*-0630@` + Storage órfão do Clênio na prod.

## Próxima sessão
- **Modo:** planejamento
- **Ler primeiro:** `plans/roadmap/roadmap-polimento.md` (continuar de #16). Specs prontos p/ execução quando for a hora: `spec-orcamento-cluster.md`, `spec-odontograma-referencia.md`.
