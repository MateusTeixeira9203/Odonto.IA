# Handoff — 2026-06-28 09:48

## Plano / spec de referência
- **Plano ativo:** `C:\Users\mateu\.claude\plans\snug-mixing-hopcroft.md` (Fase 1 retenção, Workstream A).
- **Spec desta sessão:** `plans/spec-A-onboarding-persona.md` — **aprovada e implementada** (decisão técnica A travada com o fundador).

## O que trabalhamos
- **Descoberta crítica:** o handoff anterior (2026-06-24) marcava E/A/F/B como "feitos", mas o **Workstream A (onboarding novo) nunca foi commitado** — em nenhuma branch, sem stash. `persona.ts` e `DexMark` sobreviveram (commit 29b28a4) mas **órfãos**. O onboarding no disco era o fluxo ANTIGO (`plano→form→procedimentos→sucesso`). Registrado na memória `project_fase1_rebuild.md`.
- **Aplicadas 3 migrações na prod** (`zenfemoxvwerplrjgfqz`): 079 (`dentistas.foco_principal`), 080 (`clinicas.onboarding_completo`), **081 (nova — `complete_onboarding` aceita `p_foco_principal`)**.
- **Reconstruído o Workstream A inteiro** (código local) seguindo a spec aprovada.
- **Corrigidos os 2 bugs** que o fundador apontou no cadastro/plano.
- **Removido o PostHog** do código (a pedido).

## O que concluímos
- **Migrações 079/080/081:** aplicadas e verificadas na prod. 081 é backward-compatible (dropou a função de 9 args, criou a de 10 com `p_foco_principal DEFAULT NULL` — chamadas antigas resolvem com foco=NULL, prod não quebra antes do deploy).
- **Workstream A (código):** nova máquina de passos (`identidade+persona → aha → plano → procedimentos → sucesso`), actions `iniciarOnboarding`/`definirPlano`/`marcarOnboardingCompleto`, guard via `onboarding_completo`, fiação de `foco_principal` no `getDentistaCached`, round-trip da demo (`?from=onboarding` → `?step=plano`), copy por persona no aha e no sucesso.
- **Bug #1** (cadastro mostrava plano antes de escolher): corrigido + **verificado deslogado** (mostra "Comece grátis · sem cartão", sem plano).
- **Bug #2** (badge POPULAR atrás do ✓): corrigido — badge em `top-left`, ✓ em `top-right` (cantos opostos). **Não confirmado visualmente** (ver pendências).
- **typecheck + lint limpos** nos arquivos tocados.

**Status geral:** Parcial — código do A pronto e **local/não commitado**; E2E verificado em parte (DB + UI parcial), resto fica pro teste manual do fundador.

## Decisões tomadas
| Decisão | Alternativa descartada | Motivo |
|---|---|---|
| Gravar `foco_principal` via param na RPC (081) | UPDATE pós-RPC | Atômico, não depende de policy RLS; custo de 1 migração foi aceito |
| Aplicar migrações direto na prod | Branch Supabase / staging | Só existe 1 projeto (prod); migrações aditivas e idempotentes, risco baixo |
| Aha usa `DexMark` inline + `promessaAha` | Reusar o `DexGuide` completo | Desacopla A de F; F refina identidade/roteiro do DEX depois |
| Remover PostHog do código | Só esvaziar env | Fundador não vai usar; em dev já era no-op, sobrava peso no bundle |

## Desvios do plano original
| Item do plano | O que fizemos diferente | Impacto |
|---|---|---|
| Aha "reusar DexGuide (point_demo→in_demo)" | Aha inline com `DexMark` + `promessaAha`; demo mantém seu coach próprio | Nenhum funcional — F faz o polish do DEX no aha depois |
| "Suavizar auto-dismiss do card de primeiros passos" (B2) | Não tocado (é workstream B) | Fora de escopo desta sessão |

## Erros encontrados e como pensei em resolver
| Erro / problema | Causa | Como resolvi / ia resolver | Resolvido? |
|---|---|---|---|
| Onboarding novo ausente do código | Trabalho de 2026-06-24 nunca commitado (perdido) | Reconstruir a partir da spec + plano salvos | sim (rebuild) |
| RPC `complete_onboarding` não aceitava persona | Era de 9 args | Migração 081 drop+create com 10º param backward-compat | sim |
| Especialidade (Radix Select) não responde a click/eval sintético no preview | Radix usa pointer/focus capture — limitação do harness, **não bug** | Run veterano provou que funciona de verdade (DB gravou especialidade); iniciante idem | n/a (limitação de teste) |
| Preview "racy" (snapshot mostrava plano, URL mostrava dashboard) | Storm de HMR durante as edições + transições rápidas | Verifiquei o data layer via SQL (determinístico) em vez de snapshots | contornado |
| Dúvida: fluxo auto-avançava? | — | DB confirmou: iniciante parou no meio com `onboarding_completo=false`; veterano completou com `true`. Sem auto-avanço | sim (descartado) |

## Arquivos alterados
| Arquivo | Mudança |
|---|---|
| supabase/migrations/...081_complete_onboarding_foco.sql | **novo** — RPC aceita `p_foco_principal` (aplicado na prod) |
| src/app/onboarding/actions.ts | `iniciarOnboarding`/`definirPlano`/`marcarOnboardingCompleto` (substitui `completeOnboarding`) |
| src/app/onboarding/_components/onboarding-client.tsx | nova máquina de passos + persona + aha (DexMark) + bug #2 |
| src/app/onboarding/page.tsx | lê `?step`, passa `initialStep`/`focoInicial`/`nomeInicial`, sem `?plano` |
| src/app/onboarding/layout.tsx | guard via `clinicas.onboarding_completo` |
| src/lib/get-dentista.ts | carrega `foco_principal` (Workstream E) |
| src/app/consulta/demo/page.tsx | lê `?from=onboarding` → `retornoOnboarding` |
| src/app/consulta/[agendamentoId]/_components/consulta-client.tsx | CTA "Continuar configuração" → `/onboarding?step=plano` |
| src/app/(auth)/cadastro/page.tsx + _components/cadastro-form.tsx | bug #1 — remove plano do cadastro |
| src/app/layout.tsx + (deletados) providers/PostHog*.tsx + package.json | PostHog removido |
| .env.local | preenchido/organizado (gitignored) |
| plans/spec-A-onboarding-persona.md | **novo** — spec do A |

## O que ficou pra próxima sessão
1. **[CRÍTICO] Nada está commitado nem deployado.** Todo o rebuild do A é **local**. A prod ainda roda o onboarding ANTIGO (migrações já estão lá, harmless). Quando o fundador aprovar o teste: `git add` + commit + deploy. Só aí o fluxo novo vai ao ar.
2. **[CRÍTICO] Teste manual do fundador** (ele vai fazer ao voltar de viagem) → me passar os pontos a corrigir.
3. **[ALTO] Cleanup de 2 contas de teste na prod:** `e2e-vet-0628@example.com` (Consultório Teste Veterano, onboarding completo) e `e2e-ini-0628@example.com` (Clínica Teste Iniciante, meio do fluxo). SQL/admin pronto — só pedir.
4. **[ALTO] Confirmar visualmente o bug #2** (badge POPULAR vs ✓ no card de plano) — não consegui screenshot (model classifier ficou indisponível no fim).
5. **[MÉDIO] Workstreams seguintes:** F (identidade/roteiro do DEX no aha), B (recompensa pós-ficha + card investimento), C (régua de e-mails), D (relatório de valor).
6. **[MÉDIO] design-review + impeccable-design-polish** nos passos novos (identidade/aha/plano), dark e light.

## O que eu estava planejando / cogitando
- **Aha:** hoje é uma tela limpa com DexMark + promessa. No F dá pra trazer o roteiro do DEX (DexGuide) e expressões reagindo. Decidir SE o aha deve ter mais "fala" do DEX ou ficar minimalista (outdoor test).
- **`definirPlano` atualiza `limite_dentistas`** (SOLO=1/CLINICA=5) — não validei se a policy RLS de update de `clinicas` cobre essas colunas; o E2E não bateu nesse caminho (ambas personas ficaram em SOLO). Conferir ao testar CLINICA.
- **"Importar minha tabela"** no passo procedimentos marca `onboarding_completo=true` e navega pras configs — confirmar se é o comportamento desejado (vs deixar incompleto até importar).
- **Heurística de minutos economizados** (Workstream B) continua em aberto — calibrar com dados reais.

## Como retomar
```bash
cd "C:/Users/mateu/Desktop/Odonto.IA-main"
npm run dev   # testar onboarding nas 2 personas (cadastro deslogado → identidade+persona → aha → demo → plano → procedimentos → sucesso)
# verificar bug #2 no passo 'plano' (badge POPULAR no canto esq, ✓ no dir)
# se ok: git add + commit + deploy
# cleanup contas de teste: pedir (SQL pronto)
```

## Dívidas técnicas registradas
- [ ] Rebuild do A não commitado — risco de re-perda (igual ao que aconteceu). **Commitar cedo.**
- [ ] 2 contas de teste `@example.com` na prod — limpar.
- [ ] `set-state-in-effect` pré-existente (`useDexGuide.ts:29`, etc.) — dívida do codebase, não introduzida aqui.
- [ ] Lint do repo cheio tem 36 errors pré-existentes (middleware, provider, whatsapp...) — fora do escopo.
- [ ] Bug #2 e copy do aha por persona (crescer) não confirmados visualmente — confiança por construção (mesmo code path / cantos opostos).
